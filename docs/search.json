[
  {
    "objectID": "index.html#set-parameters",
    "href": "index.html#set-parameters",
    "title": "Proportional Groundfish Redistribution",
    "section": "2. Set Parameters",
    "text": "2. Set Parameters\nAssign labels and timeframe\n\n\nCode\n# Parameters\nspp_title = \"Species\"\ntitle_name = \"Closure\"\ntimeframe &lt;- c(2011:2023)"
  },
  {
    "objectID": "index.html#spatial-data-and-grid-setup",
    "href": "index.html#spatial-data-and-grid-setup",
    "title": "Proportional Groundfish Redistribution",
    "section": "3. Spatial Data and Grid Setup",
    "text": "3. Spatial Data and Grid Setup\nLoading the Alaska background and statistical area shapefiles to create the time-series grid.\n\n\nCode\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\nstat_areas$STAT_AREA &lt;- as.numeric(stat_areas$STAT_AREA)\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "index.html#closure-assignment",
    "href": "index.html#closure-assignment",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "4. Closure Assignment",
    "text": "4. Closure Assignment\nDefining the closure area based on the statistical areas to reduce bycatch of Species A (665730, 665700, 655730, 655700). We will later look at the trade-off with the closure’s effect on Species B.\n\n\nCode\nclosure.stat.areas &lt;- c(665730, 665700, 655730, 655700)\n\nclosure &lt;- subset(stat_areas, stat_areas$STAT_AREA %in% closure.stat.areas)\nclosure$closed &lt;- \"yes\"\nclosure2 &lt;- terra::aggregate(closure)\n\nnon_closure &lt;- stat_areas[!(stat_areas$STAT_AREA %in% closure.stat.areas), ]\nnon_closure$closed &lt;- \"no\""
  },
  {
    "objectID": "index.html#simulation-data",
    "href": "index.html#simulation-data",
    "title": "Proportional Groundfish Redistribution",
    "section": "5. Simulation Data",
    "text": "5. Simulation Data\nSimulating weekly catch data for 20 cells closest to the closure area. This is where you would also enter your own catch data. Here, we’re also simulating a bias of higher PSC inside the hypothetical closure.\n\n\nCode\n# --- 5. Simulation Data (Biased for high PSC inside closure) ---\n\nset.seed(123)\nnum_to_select &lt;- 20\n\n# 1. Calculate distances from non-closure to closure areas\ndistances_to_closure &lt;- terra::distance(non_closure, closure2)\nmin_distances &lt;- apply(distances_to_closure, MARGIN = 1, FUN = min)\nnon_closure$dist_to_closure &lt;- min_distances\n\n# 2. Select 20 areas outside but close to the closure\nnon_closure_ordered &lt;- non_closure[order(non_closure$dist_to_closure), ]\nsim_areas &lt;- non_closure_ordered %&gt;% slice(1:num_to_select)\n\n# 3. Merge back closure areas\nsim_areas &lt;- rbind(sim_areas, closure)\n\n# 4. Generate Weekly Data with PSC Bias\nsimulated_years &lt;- c(2021, 2022, 2023)\nsim.list &lt;- list()\n\nfor(i in simulated_years){\n  \n  # Create a sequence of 5 weeks starting in August for each year\n  week_sequence &lt;- seq.Date(from = as.Date(paste0(i, \"-08-01\")), \n                            by = \"week\", length.out = 5)\n  year_weeks_list &lt;- list()\n  \n  for(week_date in as.character(week_sequence)) {\n    \n    # Identify which rows are inside the closure for this specific loop\n    is_inside &lt;- sim_areas$STAT_AREA %in% closure.stat.areas\n    num_inside &lt;- sum(is_inside)\n    num_outside &lt;- sum(!is_inside)\n    \n    # Create the data frame\n    wk_dat &lt;- data.frame(\n      STAT_AREA = sim_areas$STAT_AREA,\n      YEAR = i,\n      WEEK_END_DATE = week_date,\n      CATCH_ACTIVITY_DATE = as.character(as.Date(week_date) - 2),\n      PROCESSING_SECTOR = \"X\"\n    )\n    \n    # Generate Groundfish catch (kept consistent across all areas)\n    wk_dat$GF_TOTAL_CATCH_MT &lt;- round(rnorm(nrow(sim_areas), mean = 50, sd = 10), 2)\n    \n    # Higher PSC mean for inside areas \n    wk_dat$PSC_TOTAL_COUNT[is_inside] &lt;- round(rnorm(num_inside, mean = 20, sd = 3), 2)\n    \n    # Lower PSC mean for outside areas\n    wk_dat$PSC_TOTAL_COUNT[!is_inside] &lt;- rnbinom(num_outside, mu = 10, size = 2)\n    \n    year_weeks_list[[week_date]] &lt;- wk_dat\n  }\n  sim.list[[as.character(i)]] &lt;- do.call(rbind, year_weeks_list)\n}\n\ncatch_data &lt;- do.call(rbind, sim.list)\n\n# 5. Merge catch data with spatvector\n# Using all.x = FALSE ensures the map only displays areas with data\ncatch_data2 &lt;- merge(stat_areas, catch_data, by = \"STAT_AREA\", all.x = FALSE)"
  },
  {
    "objectID": "index.html#visualizations",
    "href": "index.html#visualizations",
    "title": "Proportional Groundfish Redistribution",
    "section": "6. Visualizations",
    "text": "6. Visualizations\nWeekly groundfish catch - simulated to be randomly distributed across the study area (closure + 20 adjacent stat areas).\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = GF_TOTAL_CATCH_MT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Catch (MT)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 2: Simulated weekly groundfish catch distributions.\n\n\n\n\n\nWeekly PSC catch - simulated to have higher PSC within the hypothetical closure.\n\n\nCode\nggplot() +\n  # Use subsetting [ ] to filter the SpatVector rows for Species_A\n  tidyterra::geom_spatvector(data = catch_data2[catch_data2$SPECIES == \"Species_A\", ], \n                             aes(fill = PSC_TOTAL_COUNT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  \n  scale_fill_distiller(palette = \"RdYlBu\", name = \"PSC Catch (Number)\") +\n  \n  # Keep the closure overlay (this is independent of species)\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  \n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + \n  theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 3: Simulated weekly PSC distributions."
  },
  {
    "objectID": "index.html#assign-closure-dates-isolate-impacted-hauls",
    "href": "index.html#assign-closure-dates-isolate-impacted-hauls",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "7. Assign Closure Dates & Isolate Impacted Hauls",
    "text": "7. Assign Closure Dates & Isolate Impacted Hauls\nFor this exercise, January 1 was chosen for a simple year-long closure.\n\n\nCode\n#| label: impacted-hauls\n#| echo: false\n\n#sum status quo PSC by year for later joins\nall_psc_status_quo_annual &lt;- as.data.frame(catch_data) %&gt;%\n  group_by(YEAR, SPECIES) %&gt;%\n  reframe(\n    sum_psc_status_quo = sum(PSC_TOTAL_COUNT, na.rm = TRUE)\n  )\n#assign dates to data ---\n#set closure date\nclosure_dates &lt;- data.frame(\n  YEAR = c(2021,2022,2023),\n  PROCESSING_SECTOR = \"X\",\n  closure_date = c('2021-01-01','2022-01-01', '2023-01-01')\n)\n\n# 1. Join dates to the data\nall_psc_closures_dates &lt;- as.data.frame(catch_data) %&gt;%\n  left_join(closure_dates, by = c(\"YEAR\", \"PROCESSING_SECTOR\"))\n\n# 2. Assign impacts based on dates\nall_psc_closures_dates &lt;- all_psc_closures_dates %&gt;%\n  mutate(\n    # Ensure dates are actually Date objects for the comparison to work\n    CATCH_ACTIVITY_DATE = as.Date(CATCH_ACTIVITY_DATE),\n    closure_date = as.Date(closure_date),\n    \n    impacted = ifelse(CATCH_ACTIVITY_DATE &gt; closure_date, \"yes\", \"no\"),\n    \n    # Calculate tonnage bins\n    tons_gf_after_closure = ifelse(impacted == \"yes\", GF_TOTAL_CATCH_MT, 0),\n    tons_gf_no_closure    = ifelse(impacted == \"no\" | is.na(impacted), GF_TOTAL_CATCH_MT, 0)\n  )\n\n# 3. Isolate impacted hauls (Keeping SPECIES column intact)\nall_psc_impacted &lt;- subset(all_psc_closures_dates, impacted == \"yes\")"
  },
  {
    "objectID": "index.html#calculate-catch-and-proportions-insideoutside-closure",
    "href": "index.html#calculate-catch-and-proportions-insideoutside-closure",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "8. Calculate Catch and Proportions Inside/Outside Closure",
    "text": "8. Calculate Catch and Proportions Inside/Outside Closure\nThis step summarizes the data used in the main calculations - the proportions of outside groundish catch where displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch, the total catch inside the closure to be applied to the proportions, the new PSC as a result of the new GF catch applied to local PSC rates, and the PSC caught inside the closure that will be subtracted from the net total.\n\n\nCode\n#| label: rates-props\n#| echo: false\n\n# Identify Inside/Outside after a closure would have occurred  ---\n\n#identify the area\nstat_areas_closed &lt;- closure.stat.areas \n\n#identify which hauls occurred inside/outside the closed area\nall_psc_impacted_cluster &lt;- all_psc_impacted %&gt;%\n  mutate(inside = ifelse(STAT_AREA %in% stat_areas_closed, \"yes\", \"no\"))\n\n\n# -- inside catch -- #\n#isolate hauls occurring inside closed area\ngf_inside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"yes\")\n\n#sum gf and psc inside closed area for later calcs\ngf_inside_summary &lt;- all_psc_impacted_cluster %&gt;%\n  filter(inside == \"yes\") %&gt;%\n  group_by(SPECIES, YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_inside_wk = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    psc_measure_inside_wk = sum(PSC_TOTAL_COUNT, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n# Calculate mean PSC rates per Stat Area (Outside only)\nsum.stat.timeseries.gf_all &lt;- all_psc_impacted_cluster %&gt;%\n  filter(inside == \"no\") %&gt;%\n  group_by(SPECIES, YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_wk = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    mean_psc_wk = mean(PSC_TOTAL_COUNT, na.rm = TRUE),\n    mean_gf_tons_wk = mean(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  mutate(mean_psc_rate_wk = mean_psc_wk / mean_gf_tons_wk)\n\n# Calculate total catch outside (denominator for proportions)\ngf.outside.total &lt;- all_psc_impacted_cluster %&gt;%\n  filter(inside == \"no\") %&gt;%\n  group_by(SPECIES, YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_outside_total = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\n# Join and calculate the redistribution proportion\nsum.stat.timeseries.gf_all3 &lt;- sum.stat.timeseries.gf_all %&gt;%\n  left_join(gf.outside.total, by = c(\"SPECIES\", \"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(proportion_outside_wk = tons_gf_wk / tons_gf_outside_total)"
  },
  {
    "objectID": "index.html#plot-weekly-proportions",
    "href": "index.html#plot-weekly-proportions",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "9. Plot Weekly Proportions",
    "text": "9. Plot Weekly Proportions\nThis plot shows the proportions of groundfish catch outside of the closure. These proportions will be what displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch.\n\n\nCode\n#proportion\nsum.stat.timeseries.gf_all_shp &lt;- dplyr::left_join(stat.areas.timeseries, sum.stat.timeseries.gf_all3, by=c(\"YEAR\", \"STAT_AREA\"))\nsum.stat.timeseries.gf_all_shp &lt;- terra::na.omit(sum.stat.timeseries.gf_all_shp, \"PROCESSING_SECTOR\")\n\npropdat &lt;- sum.stat.timeseries.gf_all_shp\npropdat$label &lt;- round(propdat$proportion_outside_wk, 2)\n\nggplot() +\n  tidyterra::geom_spatvector(data=subset(propdat, propdat$proportion_outside_wk&gt;0), aes(fill=proportion_outside_wk), alpha = 0.8, col=NA, na.rm=TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Proportion of GF Catch\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=4) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 4: Simulated weekly GF Catch Proportions."
  },
  {
    "objectID": "index.html#calculate-weekly-and-annual-summary-statistics",
    "href": "index.html#calculate-weekly-and-annual-summary-statistics",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "10. Calculate Weekly and Annual Summary Statistics",
    "text": "10. Calculate Weekly and Annual Summary Statistics\n\n\nCode\n#| label: summary-stats\n#| echo: false\n\n# Join catch from inside area (Matching by Species)\ntotal.join &lt;- dplyr::left_join(\n    sum.stat.timeseries.gf_all3,\n    gf_inside_summary %&gt;% dplyr::select(SPECIES, YEAR, WEEK_END_DATE, PROCESSING_SECTOR, tons_gf_inside_wk),\n    by = c(\"SPECIES\", \"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")\n  ) %&gt;%\n  mutate(\n    # NAs to 0s for weeks with no inside catch\n    tons_gf_inside_wk = tidyr::replace_na(tons_gf_inside_wk, 0),\n    # Multiply displaced catch by the outside proportions\n    displaced_gf_catch_wk = tons_gf_inside_wk * proportion_outside_wk,\n    # Calculate new PSC incurred in the new areas\n    new_PSC_wk = displaced_gf_catch_wk * mean_psc_rate_wk\n  )\n\n\ntotal.join2 &lt;- total.join %&gt;% \n  group_by(SPECIES, YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_inside_wk = max(tons_gf_inside_wk),\n    displaced_gf_catch_wk = sum(displaced_gf_catch_wk),\n    new_PSC_wk = sum(new_PSC_wk),\n    mean_psc_rate_wk = mean(mean_psc_rate_wk),\n    .groups = \"drop\"\n  )\n\n# Join the status quo inside psc numbers (Matching by Species)\ntotal.join3 &lt;- dplyr::left_join(\n    total.join2,\n    gf_inside_summary %&gt;% dplyr::select(SPECIES, YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n    by = c(\"SPECIES\", \"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")\n  ) %&gt;%\n  mutate(\n    psc_measure_inside_wk = tidyr::replace_na(psc_measure_inside_wk, 0),\n    # The \"Magic Number\": New outside PSC minus what we saved inside\n    net_change_PSC_wk = new_PSC_wk - psc_measure_inside_wk\n  )\n\ntotal.join.plot.annual &lt;- total.join3 %&gt;%\n  group_by(SPECIES, YEAR) %&gt;%\n  summarise(\n    est_change_fleet = sum(net_change_PSC_wk),\n    .groups = \"drop\"\n  )"
  },
  {
    "objectID": "index.html#status-quo-comparison",
    "href": "index.html#status-quo-comparison",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Status Quo Comparison",
    "text": "12. Status Quo Comparison\n\n\nCode\n# --- 12. Final Status Quo Comparison ---\n\n# 1. Clean build of the annual comparison data\ntotal.join.plot.annual_clean &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # 2. THE VERTICAL FIX: \n  # Calculate the distance from the bootstrap result to the bootstrap mean\n  # then apply THAT distance to your actual bar height (net_new_PSC)\n  mutate(\n    # Find the 'spread' of the bootstrap distribution\n    boot_mean = (upper_change + lower_change) / 2, # Using the mid-point of the bootstrap\n    half_width_lower = boot_mean - lower_change,\n    half_width_upper = upper_change - boot_mean,\n    \n    # Apply that spread relative to your ACTUAL bar height\n    ymin_val = net_new_PSC - half_width_lower,\n    ymax_val = net_new_PSC + half_width_upper\n  ) %&gt;%\n  # 3. Pivot for ggplot\n  pivot_longer(\n    cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n    names_to = \"psc_type\",\n    values_to = \"psc_amount\"\n  ) %&gt;%\n  mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", ymin_val, NA),\n    ymax = ifelse(psc_type == \"net_new_PSC\", ymax_val, NA)\n  ) %&gt;%\n  distinct()\n\n# 4. Final Plot \nggplot(total.join.plot.annual_clean, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", \n           position = position_dodge(width = 0.9)) + \n  geom_errorbar(aes(ymin = ymin, ymax = ymax), \n                position = position_dodge(width = 0.9), \n                width = 0.25, \n                na.rm = TRUE) +\n  # This ensures the bars start exactly at 0 and adds 15% space at the top for labels/CIs\n  scale_y_continuous(expand = expansion(mult = c(0, 0.15))) + \n  theme_bw() + \n  labs(title = paste0(spp_title, \": \", title_name),\n       x = \"Year\", \n       y = \"Estimated Annual PSC (# Fish)\") +\n  scale_fill_brewer(palette = \"Paired\", \n                    name = \"Scenario\", \n                    labels = c(\"Closure (with 95% CI)\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5))\n\n\n\n\n\n\n\n\nFigure 4"
  },
  {
    "objectID": "index.html#final-summary-statistics-table",
    "href": "index.html#final-summary-statistics-table",
    "title": "Proportional Groundfish Redistribution",
    "section": "13. Final Summary Statistics Table",
    "text": "13. Final Summary Statistics Table\n\n\nCode\n# Re-align with the original script's naming convention\ntotal.join.plot.annual2 &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # Add the CI bounds we calculated for the plot\n  mutate(\n    lower_diff = psc_change - lower_change,\n    upper_diff = upper_change - psc_change,\n    ymin_val = net_new_PSC - lower_diff,\n    ymax_val = net_new_PSC + upper_diff\n  )\n\n# Create the final summary table\nfinal.summary.table &lt;- total.join.plot.annual2 %&gt;% \n  dplyr::select(YEAR, psc_statusquo, net_new_PSC, ymin_val, ymax_val, psc_change)\n\n# Format for the report\nfinal.summary.table &lt;- as.data.frame(final.summary.table)\ncolnames(final.summary.table) &lt;- c(\"year\", \"PSC_status_quo\", \"PSC_closure\", \"lowerCI_closure\", \"upperCI_closure\", \"Net_Change\")\n\n# Calculate percent change\nfinal.summary.table$percent_change &lt;- (final.summary.table$Net_Change / final.summary.table$PSC_status_quo) * 100\n\n# Render as a professional HTML table for Quarto\nlibrary(knitr)\nkable(final.summary.table, \n      digits = 2, \n      caption = \"Annual PSC Comparison: Status Quo vs. Closure Scenarios\")\n\n\n\nAnnual PSC Comparison: Status Quo vs. Closure Scenarios\n\n\n\n\n\n\n\n\n\n\n\nyear\nPSC_status_quo\nPSC_closure\nlowerCI_closure\nupperCI_closure\nNet_Change\npercent_change\n\n\n\n\n2021\n1376.55\n1164.11\n1141.33\n1199.26\n-212.44\n-15.43\n\n\n2022\n1490.66\n1298.84\n1272.44\n1336.31\n-191.82\n-12.87\n\n\n2023\n1292.53\n1061.58\n1038.66\n1103.53\n-230.95\n-17.87"
  },
  {
    "objectID": "FMM.html#set-parameters",
    "href": "FMM.html#set-parameters",
    "title": "Proportional Groundfish Redistribution",
    "section": "2. Set Parameters",
    "text": "2. Set Parameters\nAssign titles, timeframe, and closures\n\n\nCode\n# Parameters\nspp_title = \"Species\"\ntitle_name = \"Closure\"\ntimeframe &lt;- c(2011:2023)\nclosure.stat.areas &lt;- c(665730, 665700, 655730, 655700)"
  },
  {
    "objectID": "FMM.html#spatial-data-and-grid-setup",
    "href": "FMM.html#spatial-data-and-grid-setup",
    "title": "Proportional Groundfish Redistribution",
    "section": "3. Spatial Data and Grid Setup",
    "text": "3. Spatial Data and Grid Setup\nLoading the Alaska background and statistical area shapefiles to create the time-series grid.\n\n\nCode\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\nstat_areas$STAT_AREA &lt;- as.numeric(stat_areas$STAT_AREA)\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "FMM.html#closure-assignment",
    "href": "FMM.html#closure-assignment",
    "title": "Proportional Groundfish Redistribution",
    "section": "4. Closure Assignment",
    "text": "4. Closure Assignment\nDefining the closure area based on specific statistical areas.\n\n\nCode\nclosure &lt;- subset(stat_areas, stat_areas$STAT_AREA %in% closure.stat.areas)\nclosure$closed &lt;- \"yes\"\nclosure2 &lt;- terra::aggregate(closure)\n\nnon_closure &lt;- stat_areas[!(stat_areas$STAT_AREA %in% closure.stat.areas), ]\nnon_closure$closed &lt;- \"no\""
  },
  {
    "objectID": "FMM.html#simulation-data",
    "href": "FMM.html#simulation-data",
    "title": "Proportional Groundfish Redistribution",
    "section": "5. Simulation Data",
    "text": "5. Simulation Data\nSimulating weekly catch data for 20 cells closest to the closure area. This is where you would also enter your own catch data. Here, we’re also simulating a bias of higher PSC inside the hypothetical closure.\n\n\nCode\n# --- 5. Simulation Data (Biased for high PSC inside closure) ---\n\nset.seed(123)\nnum_to_select &lt;- 20\n\n# 1. Calculate distances from non-closure to closure areas\ndistances_to_closure &lt;- terra::distance(non_closure, closure2)\nmin_distances &lt;- apply(distances_to_closure, MARGIN = 1, FUN = min)\nnon_closure$dist_to_closure &lt;- min_distances\n\n# 2. Select 20 areas outside but close to the closure\nnon_closure_ordered &lt;- non_closure[order(non_closure$dist_to_closure), ]\nsim_areas &lt;- non_closure_ordered %&gt;% slice(1:num_to_select)\n\n# 3. Merge back closure areas\nsim_areas &lt;- rbind(sim_areas, closure)\n\n# 4. Generate Weekly Data with PSC Bias\nsimulated_years &lt;- c(2021, 2022, 2023)\nsim.list &lt;- list()\n\nfor(i in simulated_years){\n  \n  # Create a sequence of 5 weeks starting in August for each year\n  week_sequence &lt;- seq.Date(from = as.Date(paste0(i, \"-08-01\")), \n                            by = \"week\", length.out = 5)\n  year_weeks_list &lt;- list()\n  \n  for(week_date in as.character(week_sequence)) {\n    \n    # Identify which rows are inside the closure for this specific loop\n    is_inside &lt;- sim_areas$STAT_AREA %in% closure.stat.areas\n    num_inside &lt;- sum(is_inside)\n    num_outside &lt;- sum(!is_inside)\n    \n    # Create the data frame\n    wk_dat &lt;- data.frame(\n      STAT_AREA = sim_areas$STAT_AREA,\n      YEAR = i,\n      WEEK_END_DATE = week_date,\n      CATCH_ACTIVITY_DATE = as.character(as.Date(week_date) - 2),\n      PROCESSING_SECTOR = \"X\"\n    )\n    \n    # Generate Groundfish catch (kept consistent across all areas)\n    wk_dat$GF_TOTAL_CATCH_MT &lt;- round(rnorm(nrow(sim_areas), mean = 50, sd = 10), 2)\n    \n    # Higher PSC mean for inside areas \n    wk_dat$PSC_TOTAL_COUNT[is_inside] &lt;- round(rnorm(num_inside, mean = 20, sd = 3), 2)\n    \n    # Lower PSC mean for outside areas\n    wk_dat$PSC_TOTAL_COUNT[!is_inside] &lt;- rnbinom(num_outside, mu = 10, size = 2)\n    \n    year_weeks_list[[week_date]] &lt;- wk_dat\n  }\n  sim.list[[as.character(i)]] &lt;- do.call(rbind, year_weeks_list)\n}\n\ncatch_data &lt;- do.call(rbind, sim.list)\n\n# 5. Merge catch data with spatvector\n# Using all.x = FALSE ensures the map only displays areas with data\ncatch_data2 &lt;- merge(stat_areas, catch_data, by = \"STAT_AREA\", all.x = FALSE)"
  },
  {
    "objectID": "FMM.html#visualizations",
    "href": "FMM.html#visualizations",
    "title": "Proportional Groundfish Redistribution",
    "section": "6. Visualizations",
    "text": "6. Visualizations\nWeekly groundfish catch - simulated to be randomly distributed across the study area (closure + 20 adjacent stat areas).\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = GF_TOTAL_CATCH_MT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Catch (MT)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 1: Simulated weekly groundfish catch distributions.\n\n\n\n\n\nWeekly PSC catch - simulated to have higher PSC within the hypothetical closure.\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = PSC_TOTAL_COUNT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"PSC Catch (Number)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 2: Simulated weekly PSC distributions."
  },
  {
    "objectID": "FMM.html#assign-closure-dates-isolate-impacted-hauls",
    "href": "FMM.html#assign-closure-dates-isolate-impacted-hauls",
    "title": "Proportional Groundfish Redistribution",
    "section": "7. Assign Closure Dates & Isolate Impacted Hauls",
    "text": "7. Assign Closure Dates & Isolate Impacted Hauls\nFor this exercise, July 1 chosen as the closure date for each year. For year-long closures, choose January 1.\n\n\nCode\n#| label: impacted-hauls\n#| echo: false\n\n#sum status quo PSC by year for later joins\nall_psc_status_quo_annual &lt;- as.data.frame(catch_data2) %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    sum_psc_status_quo = sum(PSC_TOTAL_COUNT)\n  )\n\n#assign dates to data ---\n#set closure date\nclosure_dates &lt;- data.frame(\n  YEAR = c(2021,2022,2023),\n  PROCESSING_SECTOR = \"X\",\n  closure_date = c('2021-07-01','2022-07-01', '2023-07-01')\n)\n\n#assign dates\nall_psc_closures_dates &lt;- as.data.frame(dplyr::left_join(catch_data2, closure_dates, by=c(\"YEAR\", \"PROCESSING_SECTOR\")))\n\n\n#assign groundfish and psc based on date ---\nall_psc_closures_dates$closure &lt;- ifelse(!is.na(all_psc_closures_dates$closure_date), \"yes\", \"no\")\nall_psc_closures_dates$impacted &lt;- ifelse(all_psc_closures_dates$CATCH_ACTIVITY_DATE &gt; all_psc_closures_dates$closure_date, \"yes\", \"no\")\nall_psc_closures_dates$tons_gf_after_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"yes\", all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\nall_psc_closures_dates$tons_gf_no_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"no\" | is.na(all_psc_closures_dates$impacted), all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\n\n\n#isolate impacted hauls\nall_psc_impacted &lt;- subset(all_psc_closures_dates, impacted==\"yes\")"
  },
  {
    "objectID": "FMM.html#calculate-catch-and-proportions-insideoutside-closure",
    "href": "FMM.html#calculate-catch-and-proportions-insideoutside-closure",
    "title": "Proportional Groundfish Redistribution",
    "section": "8. Calculate Catch and Proportions Inside/Outside Closure",
    "text": "8. Calculate Catch and Proportions Inside/Outside Closure\nThis step summarizes the data used in the main calculations - the proportions of outside groundish catch where displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch, the total catch inside the closure to be applied to the proportions, the new PSC as a result of the new GF catch applied to local PSC rates, and the PSC caught inside the closure that will be subtracted from the net total.\n\n\nCode\n#| label: rates-props\n#| echo: false\n\n# Identify Inside/Outside after a closure would have occurred  ---\n\n#identify the area\nstat_areas_closed &lt;- closure.stat.areas \n\n#identify which hauls occurred inside/outside the closed area\nall_psc_impacted_cluster &lt;- all_psc_impacted %&gt;%\n  mutate(inside = case_when(STAT_AREA %in% c(stat_areas_closed) ~ \"yes\", TRUE ~ NA_character_))\nall_psc_impacted_cluster$inside[is.na(all_psc_impacted_cluster$inside)] &lt;- \"no\" # NA cpue to 0s\n\n\n# -- inside catch -- #\n#isolate hauls occurring inside closed area\ngf_inside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"yes\")\n\n#sum gf and psc inside closed area for later calcs\ngf_inside_cluster2 &lt;- gf_inside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_inside_wk = sum(GF_TOTAL_CATCH_MT),\n    psc_measure_inside_wk = sum(PSC_TOTAL_COUNT)\n  )\n\n# -- outside catch -- #\n#isolate the hauls that occurred outside the closed area\ngf_outside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"no\")\n\ngf_outside_cluster2 &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_outside_wk = sum(GF_TOTAL_CATCH_MT),\n    psc_measure_outside_wk = sum(PSC_TOTAL_COUNT)\n  )\n\n\n# summarize mean psc rates and calculate ratio of means  ---\n\nsum.stat.timeseries.gf_all &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_wk = sum(GF_TOTAL_CATCH_MT),\n    mean_psc_wk = mean(PSC_TOTAL_COUNT),\n    mean_gf_tons_wk = mean(GF_TOTAL_CATCH_MT)) \n\n#ratio\nsum.stat.timeseries.gf_all$mean_psc_rate_wk &lt;- sum.stat.timeseries.gf_all$mean_psc_wk/sum.stat.timeseries.gf_all$mean_gf_tons_wk\n\n\n# Proportions by YEAR and STAT_AREA  ---\n\n#isolate gf catch by week\ngf.outside.stat &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_annual_stat = sum(GF_TOTAL_CATCH_MT)\n  )\n\n# sum all catch outside\ngf.outside.annually &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE,  PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_annual = sum(GF_TOTAL_CATCH_MT))\n\n#join the the sums back to main\nsum.stat.timeseries.gf_all2 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all, gf.outside.annually, by = c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\"))\nsum.stat.timeseries.gf_all3 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all2, gf.outside.stat, by = c(\"YEAR\",\"WEEK_END_DATE\", \"STAT_AREA\", \"PROCESSING_SECTOR\"))\n\n# CALC PROPORTION OF OUTSIDE CATCH FOR EACH AREA\nsum.stat.timeseries.gf_all3$proportion_outside_wk &lt;- sum.stat.timeseries.gf_all3$tons_gf_annual_stat/sum.stat.timeseries.gf_all3$tons_gf_annual\nsum.stat.timeseries.gf_all3 &lt;- as.data.frame(sum.stat.timeseries.gf_all3)"
  },
  {
    "objectID": "FMM.html#plot-weekly-proportions",
    "href": "FMM.html#plot-weekly-proportions",
    "title": "Proportional Groundfish Redistribution",
    "section": "9. Plot Weekly Proportions",
    "text": "9. Plot Weekly Proportions\nThis plot shows the proportions of groundfish catch for the 20 adjacent stat areas outside of the closure. These proportions will be what displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch.\n\n\nCode\n#proportion\nsum.stat.timeseries.gf_all_shp &lt;- dplyr::left_join(stat.areas.timeseries, sum.stat.timeseries.gf_all3, by=c(\"YEAR\", \"STAT_AREA\"))\nsum.stat.timeseries.gf_all_shp &lt;- terra::na.omit(sum.stat.timeseries.gf_all_shp, \"PROCESSING_SECTOR\")\n\npropdat &lt;- sum.stat.timeseries.gf_all_shp\npropdat$label &lt;- round(propdat$proportion_outside_wk, 2)\n\nggplot() +\n  tidyterra::geom_spatvector(data=subset(propdat, propdat$proportion_outside_wk&gt;0), aes(fill=proportion_outside_wk), alpha = 0.8, col=NA, na.rm=TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Proportion of GF Catch\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 3: Simulated weekly GF Catch Proportions."
  },
  {
    "objectID": "FMM.html#calculate-weekly-and-annual-summary-statistics",
    "href": "FMM.html#calculate-weekly-and-annual-summary-statistics",
    "title": "Proportional Groundfish Redistribution",
    "section": "10. Calculate Weekly and Annual Summary Statistics",
    "text": "10. Calculate Weekly and Annual Summary Statistics\n\n\nCode\n#| label: summary-stats\n#| echo: false\n\n#join catch from inside area\ntotal.join &lt;- dplyr::left_join(sum.stat.timeseries.gf_all3,\n                               gf_inside_cluster2 %&gt;% dplyr::select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, tons_gf_inside_wk),\n                               by=c(\"YEAR\", \"WEEK_END_DATE\",  \"PROCESSING_SECTOR\"))\n\n#NAs to 0s to remove weeks where no catch happened inside the closed area\ntotal.join$tons_gf_inside_wk[is.na(total.join$tons_gf_inside_wk)] &lt;- 0\n\n#multiply weekly displaced catch by the outside proportions\ntotal.join$displaced_gf_catch_wk &lt;- total.join$tons_gf_inside_wk * total.join$proportion_outside_wk\n\n#multiply PSC rate by new gf catch\ntotal.join$new_PSC_wk &lt;- total.join$displaced_gf_catch_wk * total.join$mean_psc_rate_wk\n\n\n#summarize by week and sector\ntotal.join2 &lt;- total.join %&gt;% group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_inside_wk = max(tons_gf_inside_wk),\n    displaced_gf_catch_wk = sum(displaced_gf_catch_wk),\n    new_PSC_wk = sum(new_PSC_wk),\n    mean_psc_rate_wk = mean(mean_psc_rate_wk))\n\n#join the status quo inside psc numbers\ntotal.join3 &lt;- dplyr::left_join(total.join2,\n                                gf_inside_cluster2 %&gt;% dplyr::select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n                                by=c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\"))\n#NAs to 0s\ntotal.join3$psc_measure_inside_wk[is.na(total.join3$psc_measure_inside_wk)] &lt;- 0\n\n#subtract the PSC caught inside the area\ntotal.join3$net_change_PSC_wk &lt;- total.join3$new_PSC_wk - total.join3$psc_measure_inside_wk\n\ntotal.join.plot &lt;- total.join3\n\n\n#sum est changes for annual numbers\ntotal.join.plot.annual &lt;- total.join.plot %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    est_change_fleet = sum(net_change_PSC_wk)\n  )"
  },
  {
    "objectID": "FMM.html#status-quo-comparison",
    "href": "FMM.html#status-quo-comparison",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Status Quo Comparison",
    "text": "12. Status Quo Comparison\n\n\nCode\n# --- 12. Final Status Quo Comparison ---\n\n# 1. Clean build of the annual comparison data\ntotal.join.plot.annual_clean &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # 2. THE VERTICAL FIX: \n  # Calculate the distance from the bootstrap result to the bootstrap mean\n  # then apply THAT distance to your actual bar height (net_new_PSC)\n  mutate(\n    # Find the 'spread' of the bootstrap distribution\n    boot_mean = (upper_change + lower_change) / 2, # Using the mid-point of the bootstrap\n    half_width_lower = boot_mean - lower_change,\n    half_width_upper = upper_change - boot_mean,\n    \n    # Apply that spread relative to your ACTUAL bar height\n    ymin_val = net_new_PSC - half_width_lower,\n    ymax_val = net_new_PSC + half_width_upper\n  ) %&gt;%\n  # 3. Pivot for ggplot\n  pivot_longer(\n    cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n    names_to = \"psc_type\",\n    values_to = \"psc_amount\"\n  ) %&gt;%\n  mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", ymin_val, NA),\n    ymax = ifelse(psc_type == \"net_new_PSC\", ymax_val, NA)\n  ) %&gt;%\n  distinct()\n\n# 4. Final Plot \nggplot(total.join.plot.annual_clean, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", \n           position = position_dodge(width = 0.9)) + \n  geom_errorbar(aes(ymin = ymin, ymax = ymax), \n                position = position_dodge(width = 0.9), \n                width = 0.25, \n                na.rm = TRUE) +\n  # This ensures the bars start exactly at 0 and adds 15% space at the top for labels/CIs\n  scale_y_continuous(expand = expansion(mult = c(0, 0.15))) + \n  theme_bw() + \n  labs(title = paste0(spp_title, \": \", title_name),\n       x = \"Year\", \n       y = \"Estimated Annual PSC (# Fish)\") +\n  scale_fill_brewer(palette = \"Paired\", \n                    name = \"Scenario\", \n                    labels = c(\"Closure (with 95% CI)\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5))\n\n\n\n\n\n\n\n\nFigure 4"
  },
  {
    "objectID": "FMM.html#final-summary-statistics-table",
    "href": "FMM.html#final-summary-statistics-table",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Final Summary Statistics Table",
    "text": "12. Final Summary Statistics Table\n\n\nCode\n# Re-align with the original script's naming convention\ntotal.join.plot.annual2 &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # Add the CI bounds we calculated for the plot\n  mutate(\n    lower_diff = psc_change - lower_change,\n    upper_diff = upper_change - psc_change,\n    ymin_val = net_new_PSC - lower_diff,\n    ymax_val = net_new_PSC + upper_diff\n  )\n\n# Create the final summary table\nfinal.summary.table &lt;- total.join.plot.annual2 %&gt;% \n  dplyr::select(YEAR, psc_statusquo, net_new_PSC, ymin_val, ymax_val, psc_change)\n\n# Format for the report\nfinal.summary.table &lt;- as.data.frame(final.summary.table)\ncolnames(final.summary.table) &lt;- c(\"year\", \"PSC_status_quo\", \"PSC_closure\", \"lowerCI_closure\", \"upperCI_closure\", \"Net_Change\")\n\n# Calculate percent change\nfinal.summary.table$percent_change &lt;- (final.summary.table$Net_Change / final.summary.table$PSC_status_quo) * 100\n\n# Render as a professional HTML table for Quarto\nlibrary(knitr)\nkable(final.summary.table, \n      digits = 2, \n      caption = \"Annual PSC Comparison: Status Quo vs. Closure Scenarios\")\n\n\n\nAnnual PSC Comparison: Status Quo vs. Closure Scenarios\n\n\n\n\n\n\n\n\n\n\n\nyear\nPSC_status_quo\nPSC_closure\nlowerCI_closure\nupperCI_closure\nNet_Change\npercent_change\n\n\n\n\n2021\n1376.55\n1164.11\n1141.33\n1199.26\n-212.44\n-15.43\n\n\n2022\n1490.66\n1298.84\n1272.44\n1336.31\n-191.82\n-12.87\n\n\n2023\n1292.53\n1061.58\n1038.66\n1103.53\n-230.95\n-17.87"
  },
  {
    "objectID": "index.html#bootstrap",
    "href": "index.html#bootstrap",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "11. Bootstrap",
    "text": "11. Bootstrap\nTo provide a measure of uncertainty, we are bootstrapping 1,000 alternative scenarios by randomly shuffling the PSC rates where fishing is redistributed. We calculated a 95% Confidence Interval using the middle 95% of all simulated outcomes (between 2.5th and 97.5th percentiles).\n\n\nCode\n# 1. Create a base dataframe that keeps STAT_AREA and SPECIES for resampling\ndf_bs_base &lt;- total.join %&gt;%\n  left_join(gf_inside_summary %&gt;% \n              select(SPECIES, YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n            by = c(\"SPECIES\", \"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(\n    psc_measure_inside_wk = replace_na(psc_measure_inside_wk, 0),\n    displaced_gf_catch_wk = tons_gf_inside_wk * proportion_outside_wk\n  )\n\n# 2. Set Bootstrap Parameters\nset.seed(345)\nn_iterations &lt;- 1000\nspecies_list &lt;- unique(df_bs_base$SPECIES)\nall_species_boot_list &lt;- list()\n\n# 3. Run Bootstrap Loop per Species\nfor(spp in species_list) {\n  \n  # Filter base data for the specific species\n  df_spp &lt;- df_bs_base %&gt;% filter(SPECIES == spp)\n  weeks &lt;- unique(df_spp$WEEK_END_DATE)\n  \n  # Matrix to store results for this species (Rows = Iterations, Cols = Weeks)\n  spp_boot_results &lt;- matrix(NA, nrow = n_iterations, ncol = length(weeks))\n  colnames(spp_boot_results) &lt;- weeks\n  \n  for(i in 1:n_iterations) {\n    \n    # Temporary storage for new PSC calculations in this iteration\n    new_psc_iteration &lt;- numeric(nrow(df_spp))\n    \n    for(w in weeks) {\n      wk_indices &lt;- which(df_spp$WEEK_END_DATE == w)\n      \n      # Sample from the pool of rates available for THIS species in THIS week\n      psc_pool &lt;- df_spp$mean_psc_rate_wk[wk_indices]\n      \n      # Randomly assign a rate from the pool to each STAT_AREA\n      sampled_rates &lt;- sample(psc_pool, size = length(wk_indices), replace = TRUE)\n      \n      # Calculate new PSC: (Fixed Displaced Catch) * (Randomly Sampled Rate)\n      new_psc_iteration[wk_indices] &lt;- df_spp$displaced_gf_catch_wk[wk_indices] * sampled_rates\n    }\n    \n    # Summarize net change for this iteration\n    iter_summary &lt;- df_spp %&gt;%\n      mutate(new_psc_boot = new_psc_iteration) %&gt;%\n      group_by(WEEK_END_DATE) %&gt;%\n      summarise(\n        # New PSC total minus the status quo PSC that was avoided inside\n        net_change = sum(new_psc_boot) - max(psc_measure_inside_wk),\n        .groups = \"drop\"\n      )\n    \n    # Store iteration results in the matrix\n    for(j in seq_along(weeks)) {\n      spp_boot_results[i, j] &lt;- iter_summary$net_change[iter_summary$WEEK_END_DATE == weeks[j]]\n    }\n  }\n  \n  # Convert matrix to a long-format dataframe for this species\n  all_species_boot_list[[spp]] &lt;- as.data.frame(spp_boot_results) %&gt;%\n    mutate(iteration = 1:n(), SPECIES = spp) %&gt;%\n    pivot_longer(-c(iteration, SPECIES), names_to = \"WEEK_END_DATE\", values_to = \"net_change\")\n}\n\n# 4. Combine all species and calculate Annual Confidence Intervals\nboot_long &lt;- bind_rows(all_species_boot_list) %&gt;%\n  mutate(\n    WEEK_END_DATE = as.Date(WEEK_END_DATE),\n    YEAR = lubridate::year(WEEK_END_DATE)\n  )\n\n# Sum by iteration, species, and year to get annual distribution\nboot_annual_dist &lt;- boot_long %&gt;%\n  group_by(iteration, SPECIES, YEAR) %&gt;%\n  summarise(annual_psc_change = sum(net_change), .groups = \"drop\")\n\n# Calculate final CIs for the annual change\nboot_annual_ci &lt;- boot_annual_dist %&gt;%\n  group_by(SPECIES, YEAR) %&gt;%\n  summarise(\n    mean_change = mean(annual_psc_change),\n    lower_change = quantile(annual_psc_change, 0.025),\n    upper_change = quantile(annual_psc_change, 0.975),\n    .groups = \"drop\"\n  )"
  },
  {
    "objectID": "FMM2.html#set-parameters",
    "href": "FMM2.html#set-parameters",
    "title": "Proportional Groundfish Redistribution",
    "section": "2. Set Parameters",
    "text": "2. Set Parameters\nAssign titles, timeframe, and closures\n\n\nCode\n# Parameters\nspp_title = \"Species\"\ntitle_name = \"Closure\"\ntimeframe &lt;- c(2011:2023)\nclosure.stat.areas &lt;- c(665730, 665700, 655730, 655700)"
  },
  {
    "objectID": "FMM2.html#spatial-data-and-grid-setup",
    "href": "FMM2.html#spatial-data-and-grid-setup",
    "title": "Proportional Groundfish Redistribution",
    "section": "3. Spatial Data and Grid Setup",
    "text": "3. Spatial Data and Grid Setup\nLoading the Alaska background and statistical area shapefiles to create the time-series grid.\n\n\nCode\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\nstat_areas$STAT_AREA &lt;- as.numeric(stat_areas$STAT_AREA)\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "FMM2.html#closure-assignment",
    "href": "FMM2.html#closure-assignment",
    "title": "Proportional Groundfish Redistribution",
    "section": "4. Closure Assignment",
    "text": "4. Closure Assignment\nDefining the closure area based on specific statistical areas.\n\n\nCode\nclosure &lt;- subset(stat_areas, stat_areas$STAT_AREA %in% closure.stat.areas)\nclosure$closed &lt;- \"yes\"\nclosure2 &lt;- terra::aggregate(closure)\n\nnon_closure &lt;- stat_areas[!(stat_areas$STAT_AREA %in% closure.stat.areas), ]\nnon_closure$closed &lt;- \"no\""
  },
  {
    "objectID": "FMM2.html#simulation-data",
    "href": "FMM2.html#simulation-data",
    "title": "Proportional Groundfish Redistribution",
    "section": "5. Simulation Data",
    "text": "5. Simulation Data\nSimulating weekly catch data for 20 cells closest to the closure area. This is where you would also enter your own catch data. Here, we’re also simulating a bias of higher PSC inside the hypothetical closure.\n\n\nCode\n# --- 5. Simulation Data (Biased for high PSC inside closure) ---\n\nset.seed(123)\nnum_to_select &lt;- 20\n\n# 1. Calculate distances from non-closure to closure areas\ndistances_to_closure &lt;- terra::distance(non_closure, closure2)\nmin_distances &lt;- apply(distances_to_closure, MARGIN = 1, FUN = min)\nnon_closure$dist_to_closure &lt;- min_distances\n\n# 2. Select 20 areas outside but close to the closure\nnon_closure_ordered &lt;- non_closure[order(non_closure$dist_to_closure), ]\nsim_areas &lt;- non_closure_ordered %&gt;% slice(1:num_to_select)\n\n# 3. Merge back closure areas\nsim_areas &lt;- rbind(sim_areas, closure)\n\n# 4. Generate Weekly Data with PSC Bias\nsimulated_years &lt;- c(2021, 2022, 2023)\nsim.list &lt;- list()\n\nfor(i in simulated_years){\n  \n  # Create a sequence of 5 weeks starting in August for each year\n  week_sequence &lt;- seq.Date(from = as.Date(paste0(i, \"-08-01\")), \n                            by = \"week\", length.out = 5)\n  year_weeks_list &lt;- list()\n  \n  for(week_date in as.character(week_sequence)) {\n    \n    # Identify which rows are inside the closure for this specific loop\n    is_inside &lt;- sim_areas$STAT_AREA %in% closure.stat.areas\n    num_inside &lt;- sum(is_inside)\n    num_outside &lt;- sum(!is_inside)\n    \n    # Create the data frame\n    wk_dat &lt;- data.frame(\n      STAT_AREA = sim_areas$STAT_AREA,\n      YEAR = i,\n      WEEK_END_DATE = week_date,\n      CATCH_ACTIVITY_DATE = as.character(as.Date(week_date) - 2),\n      PROCESSING_SECTOR = \"X\"\n    )\n    \n    # Generate Groundfish catch (kept consistent across all areas)\n    wk_dat$GF_TOTAL_CATCH_MT &lt;- round(rnorm(nrow(sim_areas), mean = 50, sd = 10), 2)\n    \n    # Generate PSC catch with Bias:\n    wk_dat$PSC_TOTAL_COUNT &lt;- NA # Initialize\n    \n    # Higher PSC mean for inside areas (e.g., mean = 15)\n    wk_dat$PSC_TOTAL_COUNT[is_inside] &lt;- round(rnorm(num_inside, mean = 10, sd = 3), 2)\n    \n    # Lower PSC mean for outside areas (e.g., mean = 5)\n    wk_dat$PSC_TOTAL_COUNT[!is_inside] &lt;- round(rnorm(num_outside, mean = 5, sd = 10), 2)\n    \n    year_weeks_list[[week_date]] &lt;- wk_dat\n  }\n  sim.list[[as.character(i)]] &lt;- do.call(rbind, year_weeks_list)\n}\n\ncatch_data &lt;- do.call(rbind, sim.list)\n\n# 5. Merge catch data with spatvector\n# Using all.x = FALSE ensures the map only displays areas with data\ncatch_data2 &lt;- merge(stat_areas, catch_data, by = \"STAT_AREA\", all.x = FALSE)"
  },
  {
    "objectID": "FMM2.html#visualizations",
    "href": "FMM2.html#visualizations",
    "title": "Proportional Groundfish Redistribution",
    "section": "6. Visualizations",
    "text": "6. Visualizations\nWeekly groundfish catch - simulated to be randomly distributed across the study area (closure + 20 adjacent stat areas).\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = GF_TOTAL_CATCH_MT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Catch (MT)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 1: Simulated weekly groundfish catch distributions.\n\n\n\n\n\nWeekly PSC catch - simulated to have higher PSC within the hypothetical closure.\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = PSC_TOTAL_COUNT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"PSC Catch (Number)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 2: Simulated weekly PSC distributions."
  },
  {
    "objectID": "FMM2.html#assign-closure-dates-isolate-impacted-hauls",
    "href": "FMM2.html#assign-closure-dates-isolate-impacted-hauls",
    "title": "Proportional Groundfish Redistribution",
    "section": "7. Assign Closure Dates & Isolate Impacted Hauls",
    "text": "7. Assign Closure Dates & Isolate Impacted Hauls\nFor this exercise, July 1 chosen as the closure date for each year. For year-long closures, choose January 1.\n\n\nCode\n#| label: impacted-hauls\n#| echo: false\n\n#sum status quo PSC by year for later joins\nall_psc_status_quo_annual &lt;- as.data.frame(catch_data2) %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    sum_psc_status_quo = sum(PSC_TOTAL_COUNT)\n  )\n\n#assign dates to data ---\n#set closure date\nclosure_dates &lt;- data.frame(\n  YEAR = c(2021,2022,2023),\n  PROCESSING_SECTOR = \"X\",\n  closure_date = c('2021-07-01','2022-07-01', '2023-07-01')\n)\n\n#assign dates\nall_psc_closures_dates &lt;- as.data.frame(dplyr::left_join(catch_data2, closure_dates, by=c(\"YEAR\", \"PROCESSING_SECTOR\")))\n\n\n#assign groundfish and psc based on date ---\nall_psc_closures_dates$closure &lt;- ifelse(!is.na(all_psc_closures_dates$closure_date), \"yes\", \"no\")\nall_psc_closures_dates$impacted &lt;- ifelse(all_psc_closures_dates$CATCH_ACTIVITY_DATE &gt; all_psc_closures_dates$closure_date, \"yes\", \"no\")\nall_psc_closures_dates$tons_gf_after_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"yes\", all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\nall_psc_closures_dates$tons_gf_no_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"no\" | is.na(all_psc_closures_dates$impacted), all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\n\n\n#isolate impacted hauls\nall_psc_impacted &lt;- subset(all_psc_closures_dates, impacted==\"yes\")"
  },
  {
    "objectID": "FMM2.html#calculate-catch-and-proportions-insideoutside-closure",
    "href": "FMM2.html#calculate-catch-and-proportions-insideoutside-closure",
    "title": "Proportional Groundfish Redistribution",
    "section": "8. Calculate Catch and Proportions Inside/Outside Closure",
    "text": "8. Calculate Catch and Proportions Inside/Outside Closure\nThis step summarizes the data used in the main calculations - the proportions of outside groundish catch where displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch, the total catch inside the closure to be applied to the proportions, the new PSC as a result of the new GF catch applied to local PSC rates, and the PSC caught inside the closure that will be subtracted from the net total.\n\n\nCode\n#| label: rates-props\n#| echo: false\n\n# Identify Inside/Outside after a closure would have occurred  ---\n\n#identify the area\nstat_areas_closed &lt;- closure.stat.areas \n\n#identify which hauls occurred inside/outside the closed area\nall_psc_impacted_cluster &lt;- all_psc_impacted %&gt;%\n  mutate(inside = case_when(STAT_AREA %in% c(stat_areas_closed) ~ \"yes\", TRUE ~ NA_character_))\nall_psc_impacted_cluster$inside[is.na(all_psc_impacted_cluster$inside)] &lt;- \"no\" # NA cpue to 0s\n\n\n# -- inside catch -- #\n#isolate hauls occurring inside closed area\ngf_inside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"yes\")\n\n#sum gf and psc inside closed area for later calcs\ngf_inside_cluster2 &lt;- gf_inside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_inside_wk = sum(GF_TOTAL_CATCH_MT),\n    psc_measure_inside_wk = sum(PSC_TOTAL_COUNT)\n  )\n\n# -- outside catch -- #\n#isolate the hauls that occurred outside the closed area\ngf_outside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"no\")\n\ngf_outside_cluster2 &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_outside_wk = sum(GF_TOTAL_CATCH_MT),\n    psc_measure_outside_wk = sum(PSC_TOTAL_COUNT)\n  )\n\n\n# summarize mean psc rates and calculate ratio of means  ---\n\nsum.stat.timeseries.gf_all &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_wk = sum(GF_TOTAL_CATCH_MT),\n    mean_psc_wk = mean(PSC_TOTAL_COUNT),\n    mean_gf_tons_wk = mean(GF_TOTAL_CATCH_MT)) \n\n#ratio\nsum.stat.timeseries.gf_all$mean_psc_rate_wk &lt;- sum.stat.timeseries.gf_all$mean_psc_wk/sum.stat.timeseries.gf_all$mean_gf_tons_wk\n\n\n# Proportions by YEAR and STAT_AREA  ---\n\n#isolate gf catch by week\ngf.outside.stat &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_annual_stat = sum(GF_TOTAL_CATCH_MT)\n  )\n\n# sum all catch outside\ngf.outside.annually &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE,  PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_annual = sum(GF_TOTAL_CATCH_MT))\n\n#join the the sums back to main\nsum.stat.timeseries.gf_all2 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all, gf.outside.annually, by = c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\"))\nsum.stat.timeseries.gf_all3 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all2, gf.outside.stat, by = c(\"YEAR\",\"WEEK_END_DATE\", \"STAT_AREA\", \"PROCESSING_SECTOR\"))\n\n# CALC PROPORTION OF OUTSIDE CATCH FOR EACH AREA\nsum.stat.timeseries.gf_all3$proportion_outside_wk &lt;- sum.stat.timeseries.gf_all3$tons_gf_annual_stat/sum.stat.timeseries.gf_all3$tons_gf_annual\nsum.stat.timeseries.gf_all3 &lt;- as.data.frame(sum.stat.timeseries.gf_all3)"
  },
  {
    "objectID": "FMM2.html#plot-weekly-proportions",
    "href": "FMM2.html#plot-weekly-proportions",
    "title": "Proportional Groundfish Redistribution",
    "section": "9. Plot Weekly Proportions",
    "text": "9. Plot Weekly Proportions\nThis plot shows the proportions of groundfish catch for the 20 adjacent stat areas outside of the closure. These proportions will be what displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch.\n\n\nCode\n#proportion\nsum.stat.timeseries.gf_all_shp &lt;- dplyr::left_join(stat.areas.timeseries, sum.stat.timeseries.gf_all3, by=c(\"YEAR\", \"STAT_AREA\"))\nsum.stat.timeseries.gf_all_shp &lt;- terra::na.omit(sum.stat.timeseries.gf_all_shp, \"PROCESSING_SECTOR\")\n\npropdat &lt;- sum.stat.timeseries.gf_all_shp\npropdat$label &lt;- round(propdat$proportion_outside_wk, 2)\n\nggplot() +\n  tidyterra::geom_spatvector(data=subset(propdat, propdat$proportion_outside_wk&gt;0), aes(fill=proportion_outside_wk), alpha = 0.8, col=NA, na.rm=TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Proportion of GF Catch\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 3: Simulated weekly GF Catch Proportions."
  },
  {
    "objectID": "FMM2.html#calculate-weekly-and-annual-summary-statistics",
    "href": "FMM2.html#calculate-weekly-and-annual-summary-statistics",
    "title": "Proportional Groundfish Redistribution",
    "section": "10. Calculate Weekly and Annual Summary Statistics",
    "text": "10. Calculate Weekly and Annual Summary Statistics\n\n\nCode\n#| label: summary-stats\n#| echo: false\n\n#join catch from inside area\ntotal.join &lt;- dplyr::left_join(sum.stat.timeseries.gf_all3,\n                               gf_inside_cluster2 %&gt;% dplyr::select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, tons_gf_inside_wk),\n                               by=c(\"YEAR\", \"WEEK_END_DATE\",  \"PROCESSING_SECTOR\"))\n\n#NAs to 0s to remove weeks where no catch happened inside the closed area\ntotal.join$tons_gf_inside_wk[is.na(total.join$tons_gf_inside_wk)] &lt;- 0\n\n#multiply weekly displaced catch by the outside proportions\ntotal.join$displaced_gf_catch_wk &lt;- total.join$tons_gf_inside_wk * total.join$proportion_outside_wk\n\n#multiply PSC rate by new gf catch\ntotal.join$new_PSC_wk &lt;- total.join$displaced_gf_catch_wk * total.join$mean_psc_rate_wk\n\n\n#summarize by week and sector\ntotal.join2 &lt;- total.join %&gt;% group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_inside_wk = max(tons_gf_inside_wk),\n    displaced_gf_catch_wk = sum(displaced_gf_catch_wk),\n    new_PSC_wk = sum(new_PSC_wk),\n    mean_psc_rate_wk = mean(mean_psc_rate_wk))\n\n#join the status quo inside psc numbers\ntotal.join3 &lt;- dplyr::left_join(total.join2,\n                                gf_inside_cluster2 %&gt;% dplyr::select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n                                by=c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\"))\n#NAs to 0s\ntotal.join3$psc_measure_inside_wk[is.na(total.join3$psc_measure_inside_wk)] &lt;- 0\n\n#subtract the PSC caught inside the area\ntotal.join3$net_change_PSC_wk &lt;- total.join3$new_PSC_wk - total.join3$psc_measure_inside_wk\n\ntotal.join.plot &lt;- total.join3\n\n\n#sum est changes for annual numbers\ntotal.join.plot.annual &lt;- total.join.plot %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    est_change_fleet = sum(net_change_PSC_wk)\n  )"
  },
  {
    "objectID": "FMM2.html#bootstrap",
    "href": "FMM2.html#bootstrap",
    "title": "Proportional Groundfish Redistribution",
    "section": "11. Bootstrap",
    "text": "11. Bootstrap\nTo provide a measure of uncertainty, we are bootstrapping 1,000 alternative scenarios by randomly shuffling the PSC rates where fishing is redistributed. We calculated a 95% Confidence Interval using the middle 95% of all simulated outcomes (between 2.5th and 97.5th percentiles).\n\n\nCode\n# Create a base dataframe that keeps STAT_AREA for resampling\ndf_bs_base &lt;- total.join %&gt;%\n  left_join(gf_inside_cluster2 %&gt;% \n              select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n            by = c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(psc_measure_inside_wk = replace_na(psc_measure_inside_wk, 0),\n         displaced_gf_catch_wk = tons_gf_inside_wk * proportion_outside_wk)\n\nset.seed(345)\nn_iterations &lt;- 1000\nweeks &lt;- unique(df_bs_base$WEEK_END_DATE)\nboot_results &lt;- matrix(NA, nrow = n_iterations, ncol = length(weeks))\ncolnames(boot_results) &lt;- weeks\n\nfor(i in 1:n_iterations) {\n  \n  # Temporary storage for this iteration\n  new_psc_iteration &lt;- numeric(nrow(df_bs_base))\n  \n  for(w in weeks) {\n    wk_indices &lt;- which(df_bs_base$WEEK_END_DATE == w)\n    \n    # THE FIX: Sample from the pool of all rates available this week\n    psc_pool &lt;- df_bs_base$mean_psc_rate_wk[wk_indices]\n    \n    # Randomly assign a rate from the pool to each STAT_AREA\n    sampled_rates &lt;- sample(psc_pool, size = length(wk_indices), replace = TRUE)\n    \n    # Calculate new PSC: (Fixed Displaced Catch) * (Randomly Sampled Rate)\n    new_psc_iteration[wk_indices] &lt;- df_bs_base$displaced_gf_catch_wk[wk_indices] * sampled_rates\n  }\n  \n  # Summarize results for this iteration\n  df_iter &lt;- df_bs_base\n  df_iter$new_psc_boot &lt;- new_psc_iteration\n  \n  iter_summary &lt;- df_iter %&gt;%\n    group_by(WEEK_END_DATE) %&gt;%\n    summarise(\n      net_change = sum(new_psc_boot) - max(psc_measure_inside_wk),\n      .groups = \"drop\"\n    )\n  \n  for(j in seq_along(weeks)) {\n    boot_results[i, j] &lt;- iter_summary$net_change[iter_summary$WEEK_END_DATE == weeks[j]]\n  }\n}\n\n# 4. Calculate Confidence Intervals\nci_summary &lt;- data.frame(\n  WEEK_END_DATE = colnames(boot_results),\n  mean_net_change = colMeans(boot_results),\n  lower_ci = apply(boot_results, 2, quantile, probs = 0.025),\n  upper_ci = apply(boot_results, 2, quantile, probs = 0.975)\n)\n\n# 5. Merge with original modeled data for comparison\nfinal_comparison &lt;- left_join(ci_summary, total.join3 %&gt;% \n                                select(WEEK_END_DATE, net_change_PSC_wk), \n                              by = \"WEEK_END_DATE\")\n\n#summarize bootstrap data into annual\n# Convert matrix to long dataframe to join with Year info\nboot_long &lt;- as.data.frame(boot_results) %&gt;%\n  mutate(iteration = 1:n()) %&gt;%\n  pivot_longer(-iteration, names_to = \"WEEK_END_DATE\", values_to = \"net_change\") %&gt;%\n  mutate(WEEK_END_DATE = as.Date(WEEK_END_DATE),\n         YEAR = lubridate::year(WEEK_END_DATE))\n\n# Sum by iteration and year to get annual distribution of change\nboot_annual_dist &lt;- boot_long %&gt;%\n  group_by(iteration, YEAR) %&gt;%\n  summarise(annual_psc_change = sum(net_change), .groups = \"drop\")\n\n# Calculate CIs for the annual change\nboot_annual_ci &lt;- boot_annual_dist %&gt;%\n  group_by(YEAR) %&gt;%\n  summarise(\n    lower_change = quantile(annual_psc_change, 0.025),\n    upper_change = quantile(annual_psc_change, 0.975)\n  )"
  },
  {
    "objectID": "FMM2.html#status-quo-comparison",
    "href": "FMM2.html#status-quo-comparison",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Status Quo Comparison",
    "text": "12. Status Quo Comparison\n\n\nCode\n# --- 12. Final Status Quo Comparison ---\n\n# 1. Clean build of the annual comparison data\ntotal.join.plot.annual_clean &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # 2. THE VERTICAL FIX: \n  # Calculate the distance from the bootstrap result to the bootstrap mean\n  # then apply THAT distance to your actual bar height (net_new_PSC)\n  mutate(\n    # Find the 'spread' of the bootstrap distribution\n    boot_mean = (upper_change + lower_change) / 2, # Using the mid-point of the bootstrap\n    half_width_lower = boot_mean - lower_change,\n    half_width_upper = upper_change - boot_mean,\n    \n    # Apply that spread relative to your ACTUAL bar height\n    ymin_val = net_new_PSC - half_width_lower,\n    ymax_val = net_new_PSC + half_width_upper\n  ) %&gt;%\n  # 3. Pivot for ggplot\n  pivot_longer(\n    cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n    names_to = \"psc_type\",\n    values_to = \"psc_amount\"\n  ) %&gt;%\n  mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", ymin_val, NA),\n    ymax = ifelse(psc_type == \"net_new_PSC\", ymax_val, NA)\n  ) %&gt;%\n  distinct()\n\n# 4. Final Plot \nggplot(total.join.plot.annual_clean, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", \n           position = position_dodge(width = 0.9)) + \n  geom_errorbar(aes(ymin = ymin, ymax = ymax), \n                position = position_dodge(width = 0.9), \n                width = 0.25, \n                na.rm = TRUE) +\n  # This ensures the bars start exactly at 0 and adds 15% space at the top for labels/CIs\n  scale_y_continuous(expand = expansion(mult = c(0, 0.15))) + \n  theme_bw() + \n  labs(title = paste0(spp_title, \": \", title_name),\n       x = \"Year\", \n       y = \"Estimated Annual PSC (# Fish)\") +\n  scale_fill_brewer(palette = \"Paired\", \n                    name = \"Scenario\", \n                    labels = c(\"Closure (with 95% CI)\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5))\n\n\n\n\n\n\n\n\nFigure 4"
  },
  {
    "objectID": "FMM2.html#final-summary-statistics-table",
    "href": "FMM2.html#final-summary-statistics-table",
    "title": "Proportional Groundfish Redistribution",
    "section": "13. Final Summary Statistics Table",
    "text": "13. Final Summary Statistics Table\n\n\nCode\n# Re-align with the original script's naming convention\ntotal.join.plot.annual2 &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # Add the CI bounds we calculated for the plot\n  mutate(\n    lower_diff = psc_change - lower_change,\n    upper_diff = upper_change - psc_change,\n    ymin_val = net_new_PSC - lower_diff,\n    ymax_val = net_new_PSC + upper_diff\n  )\n\n# Create the final summary table\nfinal.summary.table &lt;- total.join.plot.annual2 %&gt;% \n  dplyr::select(YEAR, psc_statusquo, net_new_PSC, ymin_val, ymax_val, psc_change)\n\n# Format for the report\nfinal.summary.table &lt;- as.data.frame(final.summary.table)\ncolnames(final.summary.table) &lt;- c(\"year\", \"PSC_status_quo\", \"PSC_closure\", \"lowerCI_closure\", \"upperCI_closure\", \"Net_Change\")\n\n# Calculate percent change\nfinal.summary.table$percent_change &lt;- (final.summary.table$Net_Change / final.summary.table$PSC_status_quo) * 100\n\n# Render as a professional HTML table for Quarto\nlibrary(knitr)\nkable(final.summary.table, \n      digits = 2, \n      caption = \"Annual PSC Comparison: Status Quo vs. Closure Scenarios\")\n\n\n\nAnnual PSC Comparison: Status Quo vs. Closure Scenarios\n\n\n\n\n\n\n\n\n\n\n\nyear\nPSC_status_quo\nPSC_closure\nlowerCI_closure\nupperCI_closure\nNet_Change\npercent_change\n\n\n\n\n2021\n724.41\n642.84\n609.98\n677.56\n-81.57\n-11.26\n\n\n2022\n841.31\n767.70\n727.32\n814.74\n-73.61\n-8.75\n\n\n2023\n691.55\n613.62\n573.09\n646.41\n-77.93\n-11.27"
  },
  {
    "objectID": "FMM.html#bootstrap",
    "href": "FMM.html#bootstrap",
    "title": "Proportional Groundfish Redistribution",
    "section": "11. Bootstrap",
    "text": "11. Bootstrap\nTo provide a measure of uncertainty, we are bootstrapping 1,000 alternative scenarios by randomly shuffling the PSC rates where fishing is redistributed. We calculated a 95% Confidence Interval using the middle 95% of all simulated outcomes (between 2.5th and 97.5th percentiles).\n\n\nCode\n# Create a base dataframe that keeps STAT_AREA for resampling\ndf_bs_base &lt;- total.join %&gt;%\n  left_join(gf_inside_cluster2 %&gt;% \n              select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n            by = c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(psc_measure_inside_wk = replace_na(psc_measure_inside_wk, 0),\n         displaced_gf_catch_wk = tons_gf_inside_wk * proportion_outside_wk)\n\nset.seed(345)\nn_iterations &lt;- 1000\nweeks &lt;- unique(df_bs_base$WEEK_END_DATE)\nboot_results &lt;- matrix(NA, nrow = n_iterations, ncol = length(weeks))\ncolnames(boot_results) &lt;- weeks\n\nfor(i in 1:n_iterations) {\n  \n  # Temporary storage for this iteration\n  new_psc_iteration &lt;- numeric(nrow(df_bs_base))\n  \n  for(w in weeks) {\n    wk_indices &lt;- which(df_bs_base$WEEK_END_DATE == w)\n    \n    # THE FIX: Sample from the pool of all rates available this week\n    psc_pool &lt;- df_bs_base$mean_psc_rate_wk[wk_indices]\n    \n    # Randomly assign a rate from the pool to each STAT_AREA\n    sampled_rates &lt;- sample(psc_pool, size = length(wk_indices), replace = TRUE)\n    \n    # Calculate new PSC: (Fixed Displaced Catch) * (Randomly Sampled Rate)\n    new_psc_iteration[wk_indices] &lt;- df_bs_base$displaced_gf_catch_wk[wk_indices] * sampled_rates\n  }\n  \n  # Summarize results for this iteration\n  df_iter &lt;- df_bs_base\n  df_iter$new_psc_boot &lt;- new_psc_iteration\n  \n  iter_summary &lt;- df_iter %&gt;%\n    group_by(WEEK_END_DATE) %&gt;%\n    summarise(\n      net_change = sum(new_psc_boot) - max(psc_measure_inside_wk),\n      .groups = \"drop\"\n    )\n  \n  for(j in seq_along(weeks)) {\n    boot_results[i, j] &lt;- iter_summary$net_change[iter_summary$WEEK_END_DATE == weeks[j]]\n  }\n}\n\n# 4. Calculate Confidence Intervals\nci_summary &lt;- data.frame(\n  WEEK_END_DATE = colnames(boot_results),\n  mean_net_change = colMeans(boot_results),\n  lower_ci = apply(boot_results, 2, quantile, probs = 0.025),\n  upper_ci = apply(boot_results, 2, quantile, probs = 0.975)\n)\n\n# 5. Merge with original modeled data for comparison\nfinal_comparison &lt;- left_join(ci_summary, total.join3 %&gt;% \n                                select(WEEK_END_DATE, net_change_PSC_wk), \n                              by = \"WEEK_END_DATE\")\n\n#summarize bootstrap data into annual\n# Convert matrix to long dataframe to join with Year info\nboot_long &lt;- as.data.frame(boot_results) %&gt;%\n  mutate(iteration = 1:n()) %&gt;%\n  pivot_longer(-iteration, names_to = \"WEEK_END_DATE\", values_to = \"net_change\") %&gt;%\n  mutate(WEEK_END_DATE = as.Date(WEEK_END_DATE),\n         YEAR = lubridate::year(WEEK_END_DATE))\n\n# Sum by iteration and year to get annual distribution of change\nboot_annual_dist &lt;- boot_long %&gt;%\n  group_by(iteration, YEAR) %&gt;%\n  summarise(annual_psc_change = sum(net_change), .groups = \"drop\")\n\n# Calculate CIs for the annual change\nboot_annual_ci &lt;- boot_annual_dist %&gt;%\n  group_by(YEAR) %&gt;%\n  summarise(\n    lower_change = quantile(annual_psc_change, 0.025),\n    upper_change = quantile(annual_psc_change, 0.975)\n  )"
  },
  {
    "objectID": "FMM_shiny.html",
    "href": "FMM_shiny.html",
    "title": "Interactive Proportional Groundfish Redistribution",
    "section": "",
    "text": "```tbkbumavilxqb #| standalone: true #| viewerHeight: 900\nlibrary(shiny) library(dplyr) library(terra) library(ggplot2) library(lubridate) library(tidyr) library(units) library(tidyterra) library(patchwork) library(data.table) library(sf)\noptions(scipen=999)\n\n— 1. DATA LOADING —\n\n\nLoad base spatial data (Ensure shapefiles/ folder is in your repo)\nstat_areas &lt;- terra::vect(“shapefiles/State_Stat_2004.shp”) stat_areas\\(STAT_AREA &lt;- as.numeric(stat_areas\\)STAT_AREA) stat_areas$area_m2 &lt;- terra::expanse(stat_areas) AK &lt;- terra::vect(“shapefiles/mv_alaska_1mil_py.shp”)\n\n\n— 2. UI DEFINITION —\nui &lt;- fluidPage( titlePanel(“Fleet Movement Model Dashboard”), sidebarLayout( sidebarPanel( sliderInput(“psc_bias”, “PSC Hotspot Bias (Multiplier):”, min = 1, max = 5, value = 2, step = 0.5), selectInput(“closure_areas”, “Select Closure Areas:”, choices = sort(unique(stat_areas$STAT_AREA)), selected = c(665730, 665700, 655730, 655700), multiple = TRUE), helpText(“Adjust the slider to simulate higher PSC density inside the chosen areas.”) ), mainPanel( tabsetPanel( tabPanel(“Weekly Catch Maps”, plotOutput(“gfPlot”, height = “600px”)), tabPanel(“Weekly PSC Maps”, plotOutput(“pscPlot”, height = “600px”)), tabPanel(“Annual Comparison”, plotOutput(“summaryPlot”)), tabPanel(“Data Table”, tableOutput(“summaryTable”)) ) ) ) )\n\n\n— 3. SERVER LOGIC —\nserver &lt;- function(input, output, session) {\nanalysis_results &lt;- reactive({ req(input\\(closure_areas, input\\)psc_bias)\ncurrent_closure_ids &lt;- as.numeric(input$closure_areas)\nbias_factor &lt;- input$psc_bias\n\n# Geometry Logic\nclosure_poly &lt;- subset(stat_areas, stat_areas$STAT_AREA %in% current_closure_ids)\nclosure_agg &lt;- terra::aggregate(closure_poly)\nnon_closure &lt;- stat_areas[!(stat_areas$STAT_AREA %in% current_closure_ids), ]\n\ndist_to_close &lt;- terra::distance(non_closure, closure_agg)\nnon_closure$dist_to_closure &lt;- apply(dist_to_close, 1, min)\nsim_areas &lt;- non_closure[order(non_closure$dist_to_closure), ] %&gt;% \n               head(20) %&gt;% \n               rbind(closure_poly)\n\n# Simulation Logic\nsimulated_years &lt;- c(2021, 2022, 2023)\nsim_list &lt;- list()\nset.seed(123)\nfor(i in simulated_years){\n  week_seq &lt;- seq.Date(from = as.Date(paste0(i, \"-08-01\")), by = \"week\", length.out = 5)\n  for(week_date in as.character(week_seq)) {\n    is_inside &lt;- sim_areas$STAT_AREA %in% current_closure_ids\n    wk_dat &lt;- data.frame(\n      STAT_AREA = sim_areas$STAT_AREA,\n      YEAR = i,\n      WEEK_END_DATE = week_date,\n      CATCH_ACTIVITY_DATE = as.character(as.Date(week_date) - 2),\n      PROCESSING_SECTOR = \"X\",\n      GF_TOTAL_CATCH_MT = round(rnorm(nrow(sim_areas), mean = 50, sd = 10), 2)\n    )\n    wk_dat$PSC_TOTAL_COUNT &lt;- ifelse(is_inside,\n      round(rnorm(sum(is_inside), mean = 5 * bias_factor, sd = 2), 2),\n      round(rnorm(sum(!is_inside), mean = 5, sd = 2), 2)\n    )\n    sim_list[[paste0(i, week_date)]] &lt;- wk_dat\n  }\n}\n\ncatch_data &lt;- do.call(rbind, sim_list)\ncatch_data2 &lt;- terra::merge(stat_areas, catch_data, by = \"STAT_AREA\", all.x = FALSE)\n\n# Redistribution Math\nclosure_dates &lt;- data.frame(\n  YEAR = c(2021, 2022, 2023),\n  PROCESSING_SECTOR = \"X\",\n  closure_date = c('2021-07-01','2022-07-01', '2023-07-01')\n)\n\ncatch_df &lt;- as.data.frame(catch_data2)\nall_psc_dates &lt;- catch_df %&gt;%\n  left_join(closure_dates, by = c(\"YEAR\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(\n    impacted = ifelse(CATCH_ACTIVITY_DATE &gt; closure_date, \"yes\", \"no\"),\n    inside = ifelse(STAT_AREA %in% current_closure_ids, \"yes\", \"no\")\n  )\n\nimpacted_hauls &lt;- all_psc_dates %&gt;% filter(impacted == \"yes\")\ngf_inside &lt;- impacted_hauls %&gt;% filter(inside == \"yes\") %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(tons_gf_inside_wk = sum(GF_TOTAL_CATCH_MT), \n            psc_inside_wk = sum(PSC_TOTAL_COUNT), .groups = \"drop\")\n\ngf_outside &lt;- impacted_hauls %&gt;% filter(inside == \"no\")\nrates_props &lt;- gf_outside %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  summarise(tons_gf_wk = sum(GF_TOTAL_CATCH_MT),\n            mean_psc_rate_wk = sum(PSC_TOTAL_COUNT)/sum(GF_TOTAL_CATCH_MT), .groups = \"drop\") %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  mutate(prop_outside_wk = tons_gf_wk / sum(tons_gf_wk)) %&gt;% ungroup()\n\nnet_change_calc &lt;- rates_props %&gt;%\n  left_join(gf_inside, by=c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(new_PSC_wk = (tons_gf_inside_wk * prop_outside_wk) * mean_psc_rate_wk) %&gt;%\n  group_by(YEAR) %&gt;%\n  summarise(psc_change = sum(new_PSC_wk, na.rm=TRUE) - sum(psc_inside_wk, na.rm=TRUE), .groups = \"drop\")\n\nstatus_quo_annual &lt;- catch_df %&gt;% \n  group_by(YEAR) %&gt;% \n  summarise(psc_statusquo = sum(PSC_TOTAL_COUNT), .groups = \"drop\")\n\nfinal_summary &lt;- status_quo_annual %&gt;%\n  left_join(net_change_calc, by=\"YEAR\") %&gt;%\n  mutate(net_new_PSC = psc_statusquo + coalesce(psc_change, 0))\n\nreturn(list(\n  catch_map = catch_data2,\n  closure_geom = closure_agg,\n  summary_df = final_summary\n))\n})\noutput\\(gfPlot &lt;- renderPlot({\n    res &lt;- analysis_results()\n    ggplot() +\n      geom_spatvector(data = res\\)catch_map, aes(fill = GF_TOTAL_CATCH_MT), alpha = 0.8) + scale_fill_distiller(palette = “GnBu”, name = “Catch (MT)”) + geom_spatvector(data = res$closure_geom, color = “red”, fill = NA, lwd = 1) + facet_wrap(~WEEK_END_DATE, ncol = 5) + theme_minimal() })\noutput\\(pscPlot &lt;- renderPlot({\n    res &lt;- analysis_results()\n    ggplot() +\n      geom_spatvector(data = res\\)catch_map, aes(fill = PSC_TOTAL_COUNT), alpha = 0.8) + scale_fill_distiller(palette = “Reds”, name = “PSC Count”) + geom_spatvector(data = res$closure_geom, color = “black”, fill = NA, lwd = 1) + facet_wrap(~WEEK_END_DATE, ncol = 5) + theme_minimal() })\noutput\\(summaryPlot &lt;- renderPlot({\n    res &lt;- analysis_results()\n    plot_data &lt;- res\\)summary_df %&gt;% pivot_longer(cols = c(“psc_statusquo”, “net_new_PSC”), names_to = “psc_type”, values_to = “psc_amount”)\nggplot(plot_data, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", position = \"dodge\") +\n  scale_fill_manual(values = c(\"psc_statusquo\" = \"#1f78b4\", \"net_new_PSC\" = \"#a6cee3\"),\n                    labels = c(\"Status Quo\", \"Closure Scenario\"), name = \"Scenario\") +\n  labs(x = \"Year\", y = \"Total PSC (# Fish)\") +\n  theme_minimal()\n})\noutput\\(summaryTable &lt;- renderTable({\n    analysis_results()\\)summary_df }) }\n\n\n— 4. LAUNCH —\nshinyApp(ui, server)"
  },
  {
    "objectID": "index.html#load-spatial-data",
    "href": "index.html#load-spatial-data",
    "title": "Proportional Groundfish Redistribution",
    "section": "3. Load Spatial Data",
    "text": "3. Load Spatial Data\nLoading the Alaska background and statistical area shapefiles to create the time-series grid.\n\n\nCode\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\n\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "index.html#load-catch-data",
    "href": "index.html#load-catch-data",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "2. Load Catch Data",
    "text": "2. Load Catch Data\nThis is where you load your catch data [‘catch_data’]. For this example, we are simulating weekly catch data for 20 stat_areas closest to a hypothetical closure area. Here, we simulate two species (A and B), where Species A has a bias of higher PSC catch inside the area and Species B has a higher PSC catch outside the area.\n\n\nCode\n#| label: simulation\n\n\n# --- 5. Simulation Data (Biased for high PSC inside a closure) ---\n\nclosure.stat.areas &lt;- c(665730, 665700, 655730, 655700)\n\nclosure &lt;- subset(stat_areas, stat_areas$STAT_AREA %in% closure.stat.areas)\nclosure$closed &lt;- \"yes\"\nclosure2 &lt;- terra::aggregate(closure)\n\nnon_closure &lt;- stat_areas[!(stat_areas$STAT_AREA %in% closure.stat.areas), ]\nnon_closure$closed &lt;- \"no\"\n\nset.seed(123)\nnum_to_select &lt;- 20\n\n# 1. Calculate distances from non-closure to closure areas\ndistances_to_closure &lt;- terra::distance(non_closure, closure2)\nmin_distances &lt;- apply(distances_to_closure, MARGIN = 1, FUN = min)\nnon_closure$dist_to_closure &lt;- min_distances\n\n# 2. Select 20 areas outside but close to the closure\nnon_closure_ordered &lt;- non_closure[order(non_closure$dist_to_closure), ]\nsim_areas &lt;- non_closure_ordered %&gt;% slice(1:num_to_select)\n\n# 3. Merge back closure areas\nsim_areas &lt;- rbind(sim_areas, closure)\n\n# 4. Generate Weekly Data with PSC Bias\nsimulated_years &lt;- c(2021, 2022, 2023)\nsim.list &lt;- list()\n\n\n# Define the two scenarios\nspecies_configs &lt;- list(\n  \"Species_A\" = list(inside_mu = 20, outside_mu = 10), # High inside\n  \"Species_B\" = list(inside_mu = 10, outside_mu = 15)  # High outside (The \"Opposite\")\n)\n\nall_species_list &lt;- list()\n\nfor(spp_name in names(species_configs)){\n  \n  conf &lt;- species_configs[[spp_name]]\n  sim.list &lt;- list()\n  \n  for(i in simulated_years){\n    week_sequence &lt;- seq.Date(from = as.Date(paste0(i, \"-08-01\")), \n                              by = \"week\", length.out = 4)\n    year_weeks_list &lt;- list()\n    \n    for(week_date in as.character(week_sequence)) {\n      is_inside &lt;- sim_areas$STAT_AREA %in% closure.stat.areas\n      num_inside &lt;- sum(is_inside)\n      num_outside &lt;- sum(!is_inside)\n      \n      wk_dat &lt;- data.frame(\n        STAT_AREA = sim_areas$STAT_AREA,\n        SPECIES = spp_name,  # Track which species this is\n        YEAR = i,\n        WEEK_END_DATE = week_date,\n        CATCH_ACTIVITY_DATE = as.character(as.Date(week_date) - 2),\n        PROCESSING_SECTOR = \"X\"\n      )\n      \n      wk_dat$GF_TOTAL_CATCH_MT &lt;- round(rnorm(nrow(sim_areas), mean = 50, sd = 10), 2)\n      \n      # Apply the bias based on the species config\n      wk_dat$PSC_TOTAL_COUNT &lt;- NA\n      # Inside Areas\n      wk_dat$PSC_TOTAL_COUNT[is_inside] &lt;- round(rnorm(num_inside, mean = conf$inside_mu, sd = 3), 2)\n      # Outside Areas (Using rnorm for both to keep the 'opposite' effect clean)\n      wk_dat$PSC_TOTAL_COUNT[!is_inside] &lt;- round(rnorm(num_outside, mean = conf$outside_mu, sd = 5), 2)\n      \n      year_weeks_list[[week_date]] &lt;- wk_dat\n    }\n    sim.list[[as.character(i)]] &lt;- do.call(rbind, year_weeks_list)\n  }\n  all_species_list[[spp_name]] &lt;- do.call(rbind, sim.list)\n}\n\n# Combine both species into one master dataframe\ncatch_data_combined &lt;- do.call(rbind, all_species_list)\n\n# Merge with SpatVector\ncatch_data &lt;- merge(stat_areas, catch_data_combined, by = \"STAT_AREA\", all.x = FALSE)"
  },
  {
    "objectID": "index.html#explore-bycatch-data",
    "href": "index.html#explore-bycatch-data",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "3. Explore Bycatch Data",
    "text": "3. Explore Bycatch Data\nIn this example, the average highest PSC catch of Species A are in areas 665730, 665700, 655730, 655700, while these areas have the lowest PSC catch for Species B.\n\n\nCode\n# This groups by STAT_AREA and calculates the mean of all other numeric columns\ncatch_data_means &lt;- st_as_sf(catch_data)\ncatch_data_means &lt;- catch_data_means %&gt;% dplyr::group_by(STAT_AREA, SPECIES) %&gt;%\n  summarise(\n    mean_psc = mean(PSC_TOTAL_COUNT)\n  )\ncatch_data_means2 &lt;- terra::vect(catch_data_means)\n\n\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data_means2, aes(fill = mean_psc), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  tidyterra::geom_spatvector_text(data = catch_data_means2, aes(label = STAT_AREA),\n                                  size = 2.5,       # Adjust size as needed\n                                  fontface = \"bold\", \n                                  check_overlap = TRUE) + # Prevents messy overlapping text\n  scale_fill_distiller(palette = \"RdYlBu\", name = \"Mean PSC Catch\") +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank()) +\n  facet_wrap(~SPECIES)\n\n\n\n\n\n\n\n\nFigure 1: Simulated weekly PSC distributions."
  },
  {
    "objectID": "index.html#compare-to-status-quo",
    "href": "index.html#compare-to-status-quo",
    "title": "Proportional Groundfish Redistribution",
    "section": "14. Compare to Status Quo",
    "text": "14. Compare to Status Quo\nIn this example, we can confidently say that the closure may reduce bycatch of Species A, but with a trade-off of increased bycatch in Species B.\n\n\nCode\n# --- 12. Final Status Quo Comparison ---\n\n# 1. Clean build of the annual comparison data (Anchored by SPECIES)\ntotal.join.plot.annual_clean &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = c(\"SPECIES\", \"YEAR\")) %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  # Join bootstrap CIs by SPECIES and YEAR\n  left_join(boot_annual_ci, by = c(\"SPECIES\", \"YEAR\")) %&gt;%\n  \n  # 2. THE VERTICAL FIX (Relative to net_new_PSC)\n  mutate(\n    half_width_lower = mean_change - lower_change,\n    half_width_upper = upper_change - mean_change,\n    \n    ymin_val = net_new_PSC - half_width_lower,\n    ymax_val = net_new_PSC + half_width_upper\n  ) %&gt;%\n  \n  # 3. Pivot for ggplot\n  pivot_longer(\n    cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n    names_to = \"psc_type\",\n    values_to = \"psc_amount\"\n  ) %&gt;%\n  mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", ymin_val, NA),\n    ymax = ifelse(psc_type == \"net_new_PSC\", ymax_val, NA)\n  )\n\n# 4. Final Plot with Faceting by Species\nggplot(total.join.plot.annual_clean, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", \n           position = position_dodge(width = 0.9)) + \n  geom_errorbar(aes(ymin = ymin, ymax = ymax), \n                position = position_dodge(width = 0.9), \n                width = 0.25, \n                na.rm = TRUE) +\n  # Add faceting so you see the \"Opposite\" effects side-by-side\n  facet_wrap(~SPECIES, scales = \"free_y\") + \n  \n  scale_y_continuous(expand = expansion(mult = c(0, 0.15))) + \n  theme_bw() + \n  labs(title = \"Annual Impact Comparison: Scenario vs. Status Quo\",\n       subtitle = \"Error bars represent 95% Bootstrap Confidence Interval\",\n       x = \"Year\", \n       y = \"Estimated Annual PSC (# Fish)\") +\n  scale_fill_brewer(palette = \"Paired\", \n                    name = \"Scenario\", \n                    labels = c(\"Closure Scenario\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5),\n        plot.subtitle = element_text(hjust = 0.5),\n        legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure 5"
  },
  {
    "objectID": "index.html#summary-statistics",
    "href": "index.html#summary-statistics",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "13. Summary Statistics",
    "text": "13. Summary Statistics\nBased on the years explored, Species A may have had roughly 12-14% reductions in bycatch, at the expense of roughly 4-7% increases in bycatch of Species B.\n\n\nCode\n# --- 13. Final Summary Table ---\n\n# 1. Re-align with the species-aware naming convention\ntotal.join.plot.annual2 &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = c(\"SPECIES\", \"YEAR\")) %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = c(\"SPECIES\", \"YEAR\")) %&gt;%\n  # 2. Add the CI bounds calculated for the plot\n  mutate(\n    # Note: Using the bootstrap bounds directly for the table\n    lowerCI_closure = net_new_PSC - (mean_change - lower_change),\n    upperCI_closure = net_new_PSC + (upper_change - mean_change)\n  )\n\n# 3. Create the final summary table\nfinal.summary.table &lt;- total.join.plot.annual2 %&gt;% \n  dplyr::select(SPECIES, YEAR, psc_statusquo, net_new_PSC, lowerCI_closure, upperCI_closure, psc_change) %&gt;%\n  mutate(percent_change = (psc_change / psc_statusquo) * 100) %&gt;%\n  arrange(SPECIES, YEAR) # Group species together for the report\n\n# 4. Format for the report\nfinal.summary.table &lt;- as.data.frame(final.summary.table)\ncolnames(final.summary.table) &lt;- c(\"Species\", \"Year\", \"PSC_Status_Quo\", \"PSC_Closure\", \n                                   \"Lower_CI\", \"Upper_CI\", \"Net_Change\", \"Percent_Change\")\n\n# 5. Render as a professional HTML table for Quarto\n\nkable(final.summary.table, \n      digits = 2, \n      caption = \"Annual PSC Comparison: Status Quo vs. Closure Scenarios by Species\",\n      format = \"html\") %&gt;%\n  kable_styling(bootstrap_options = c(\"striped\", \"hover\", \"condensed\")) %&gt;%\n  collapse_rows(columns = 1, valign = \"top\") # Visually groups Species A and B rows\n\n\n\nAnnual PSC Comparison: Status Quo vs. Closure Scenarios by Species\n\n\nSpecies\nYear\nPSC_Status_Quo\nPSC_Closure\nLower_CI\nUpper_CI\nNet_Change\nPercent_Change\n\n\n\n\nSpecies_A\n2021\n1139.28\n976.84\n961.39\n992.34\n-162.44\n-14.26\n\n\n2022\n1132.31\n998.01\n977.93\n1018.08\n-134.30\n-11.86\n\n\n2023\n1160.12\n1016.46\n999.16\n1033.81\n-143.66\n-12.38\n\n\nSpecies_B\n2021\n1384.40\n1507.74\n1482.82\n1533.06\n123.34\n8.91\n\n\n2022\n1395.65\n1449.12\n1429.28\n1469.17\n53.47\n3.83\n\n\n2023\n1351.37\n1434.26\n1414.60\n1454.19\n82.89\n6.13"
  },
  {
    "objectID": "index.html#plot-weekly-groundfish-and-psc-catch",
    "href": "index.html#plot-weekly-groundfish-and-psc-catch",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "6. Plot Weekly Groundfish and PSC Catch",
    "text": "6. Plot Weekly Groundfish and PSC Catch\nWeekly groundfish catch - simulated to be randomly distributed across the study area (closure + 20 adjacent stat areas). These are used to calculate PSC rates per stat_area.\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data, aes(fill = GF_TOTAL_CATCH_MT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Catch (MT)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=4) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 2: Simulated weekly groundfish catch distributions.\n\n\n\n\n\nWeekly PSC catch.\n\n\nCode\nggplot() +\n  # Use subsetting [ ] to filter the SpatVector rows for Species_A\n  tidyterra::geom_spatvector(data = catch_data[catch_data$SPECIES == \"Species_A\", ], \n                             aes(fill = PSC_TOTAL_COUNT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  \n  scale_fill_distiller(palette = \"RdYlBu\", name = \"PSC Catch (Number)\") +\n  \n  # Keep the closure overlay (this is independent of species)\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  \n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=4) +\n  theme_bw() + \n  theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 3: Simulated weekly PSC distributions."
  },
  {
    "objectID": "index.html#do-we-have-the-right-area",
    "href": "index.html#do-we-have-the-right-area",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "5. Do we have the right area?",
    "text": "5. Do we have the right area?\nThe above method is a user-defined selection of stat_areas. We can also use a spatial optimization approach to allow the data to choose the stat_areas with the highest reduction in PSC. In this example, select the four adjacent stat_areas with the highest PSC catch. Because we use simulated data, this approach unsurprisingly gives us the same four areas for Species A as above.\n\n\nCode\n#| label: automated-selection-fixed\n\n# 1. Convert to sf and ensure we have a unique ID for indexing\nstat_sf &lt;- st_as_sf(stat_areas)\nstat_sf$row_id &lt;- 1:nrow(stat_sf)\n\n# 2. Define Neighbors (Contiguity)\n# 'queen = TRUE' includes corners; 'FALSE' is Rook (edges only)\nnb &lt;- st_touches(stat_sf) \n\n# 3. Create a summary table of Species A PSC\n# Ensure it matches the stat_sf order\npsc_lookup &lt;- catch_data_means %&gt;%\n  filter(SPECIES == \"Species_A\") %&gt;%\n  as_tibble()\n\n# 4. The \"Best 4\" Search Logic\nresults &lt;- list()\n\nfor(i in 1:nrow(stat_sf)) {\n  # Get the seed area ID\n  seed_area &lt;- stat_sf$STAT_AREA[i]\n  \n  # Find all neighbors of that seed\n  neighbor_indices &lt;- nb[[i]]\n  neighbor_areas &lt;- stat_sf$STAT_AREA[neighbor_indices]\n  \n  # Get PSC values for the seed + all its neighbors\n  potential_group &lt;- psc_lookup %&gt;%\n    filter(STAT_AREA %in% c(seed_area, neighbor_areas)) %&gt;%\n    arrange(desc(mean_psc))\n  \n  # If we have at least 4 areas in this \"cluster\", take the top 4\n  if(nrow(potential_group) &gt;= 4) {\n    top_4 &lt;- potential_group %&gt;% slice(1:4)\n    \n    results[[i]] &lt;- data.frame(\n      seed = seed_area,\n      total_psc = sum(top_4$mean_psc),\n      area_list = paste(top_4$STAT_AREA, collapse = \",\")\n    )\n  }\n}\n\n# Combine and find the winner\noptimization_results &lt;- do.call(rbind, results)\nwinner &lt;- optimization_results[order(-optimization_results$total_psc), ][1, ]\n\n# 5. Extract the 4 Stat Areas\nclosure.stat.areas &lt;- as.numeric(unlist(strsplit(winner$area_list, \",\")))\n\n# --- Prepare Summary Table ---\n# We want to see how the Top 4 contribute to the total PSC for BOTH species\nfinal.summary.table &lt;- catch_data_means %&gt;%\n  filter(STAT_AREA %in% closure.stat.areas) %&gt;%\n  as_tibble() %&gt;%\n  arrange(SPECIES, desc(mean_psc)) %&gt;%\n  group_by(SPECIES) %&gt;%\n  mutate(\n    Rank = row_number(),\n    cum_psc = cumsum(mean_psc)\n  ) %&gt;%\n  ungroup() %&gt;%\n  select(SPECIES, Rank, STAT_AREA, mean_psc, cum_psc)\n\n# Calculate Cluster Totals for a 'Total' row\ntotals &lt;- final.summary.table %&gt;%\n  group_by(SPECIES) %&gt;%\n  summarise(\n    STAT_AREA = \"CLUSTER TOTAL\",\n    mean_psc = sum(mean_psc),\n    Rank = NA\n  )\n\nfinal.summary.table &lt;- bind_rows(final.summary.table, totals) %&gt;%\n  arrange(SPECIES, is.na(Rank), Rank)\n\n#print results\n\n# 1. Extract the winning data\nwinner_areas &lt;- paste(closure.stat.areas, collapse = \", \")\ntotal_psc_value &lt;- round(winner$total_psc, 2)\nseed_area &lt;- winner$seed\n\n# 2. Print the dynamic sentence\n# Print the concise output\ncat(paste0(\"**Automated Selection:** \", winner_areas))\n\n\n**Automated Selection:** 655700, 665700, 655730, 665730"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "",
    "text": "This vignette evaluates a simulated trade-off in bycatch effects under a hypothetical closure scenario. All data is simulated to show process only. This is also where we load the background grid and any parameters such as timeframes. Here, we use a baseline grid of the state statistical areas used for commercial catch accounting in Alaska, and are going to simulate data to years 2021:2023.\n\n\nCode\n# Load packages\nlibrary(dplyr)\nlibrary(terra)\nlibrary(ggplot2)\nlibrary(lubridate)\nlibrary(tidyr)\nlibrary(units)\nlibrary(gridExtra)\nlibrary(tidyterra)\nlibrary(ggpubr)\nlibrary(patchwork)\nlibrary(ggpattern)\nlibrary(data.table)\nlibrary(sf)\nlibrary(kableExtra)\nlibrary(igraph)\nlibrary(knitr)\n\noptions(scipen=999)\n\n# Parameters\nspp_title = \"Species\"\ntitle_name = \"Closure\"\ntimeframe &lt;- c(2021:2023)\n\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\n\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "index.html#setup",
    "href": "index.html#setup",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "",
    "text": "This vignette evaluates a simulated trade-off in bycatch effects under a hypothetical closure scenario. All data is simulated to show process only. This is also where we load the background grid and any parameters such as timeframes. Here, we use a baseline grid of the state statistical areas used for commercial catch accounting in Alaska, and are going to simulate data to years 2021:2023.\n\n\nCode\n# Load packages\nlibrary(dplyr)\nlibrary(terra)\nlibrary(ggplot2)\nlibrary(lubridate)\nlibrary(tidyr)\nlibrary(units)\nlibrary(gridExtra)\nlibrary(tidyterra)\nlibrary(ggpubr)\nlibrary(patchwork)\nlibrary(ggpattern)\nlibrary(data.table)\nlibrary(sf)\nlibrary(kableExtra)\nlibrary(igraph)\nlibrary(knitr)\n\noptions(scipen=999)\n\n# Parameters\nspp_title = \"Species\"\ntitle_name = \"Closure\"\ntimeframe &lt;- c(2021:2023)\n\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\n\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "index.html#evaluate-impacts",
    "href": "index.html#evaluate-impacts",
    "title": "Fleet Movement Model: A Proportional Redistribution Approach to Bycatch Evaluation",
    "section": "12. Evaluate Impacts",
    "text": "12. Evaluate Impacts\nIn this example, we can confidently say that the closure may reduce bycatch of Species A, but with a trade-off of increased bycatch in Species B.\n\n\nCode\n# --- 12. Final Status Quo Comparison ---\n\n# 1. Clean build of the annual comparison data (Anchored by SPECIES)\ntotal.join.plot.annual_clean &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = c(\"SPECIES\", \"YEAR\")) %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  # Join bootstrap CIs by SPECIES and YEAR\n  left_join(boot_annual_ci, by = c(\"SPECIES\", \"YEAR\")) %&gt;%\n  \n  # 2. THE VERTICAL FIX (Relative to net_new_PSC)\n  mutate(\n    half_width_lower = mean_change - lower_change,\n    half_width_upper = upper_change - mean_change,\n    \n    ymin_val = net_new_PSC - half_width_lower,\n    ymax_val = net_new_PSC + half_width_upper\n  ) %&gt;%\n  \n  # 3. Pivot for ggplot\n  pivot_longer(\n    cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n    names_to = \"psc_type\",\n    values_to = \"psc_amount\"\n  ) %&gt;%\n  mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", ymin_val, NA),\n    ymax = ifelse(psc_type == \"net_new_PSC\", ymax_val, NA)\n  )\n\n# 4. Final Plot with Faceting by Species\nggplot(total.join.plot.annual_clean, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", \n           position = position_dodge(width = 0.9)) + \n  geom_errorbar(aes(ymin = ymin, ymax = ymax), \n                position = position_dodge(width = 0.9), \n                width = 0.25, \n                na.rm = TRUE) +\n  # Add faceting so you see the \"Opposite\" effects side-by-side\n  facet_wrap(~SPECIES, scales = \"free_y\") + \n  \n  scale_y_continuous(expand = expansion(mult = c(0, 0.15))) + \n  theme_bw() + \n  labs(title = \"Annual Impact Comparison: Scenario vs. Status Quo\",\n       subtitle = \"Error bars represent 95% Bootstrap Confidence Interval\",\n       x = \"Year\", \n       y = \"Estimated Annual PSC (# Fish)\") +\n  scale_fill_brewer(palette = \"Paired\", \n                    name = \"Scenario\", \n                    labels = c(\"Closure Scenario\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5),\n        plot.subtitle = element_text(hjust = 0.5),\n        legend.position = \"bottom\")\n\n\n\n\n\n\n\n\nFigure 5"
  },
  {
    "objectID": "fmm_validation.html",
    "href": "fmm_validation.html",
    "title": "Proportional Groundfish Redistribution",
    "section": "",
    "text": "This analysis predicts the spatial movement and net PSC change for displaced vessels under hypothetical closures of the red king crab sub savings area (RKCSSA) and validates predictions against closures in 2022 and 2023.\n\n\nCode\n#| label: setup\n#| include: false\n\n# Load packages\nlibrary(dplyr)\nlibrary(terra)\nlibrary(ggplot2)\nlibrary(lubridate)\nlibrary(tidyr)\nlibrary(units)\nlibrary(gridExtra)\nlibrary(tidyterra)\nlibrary(ggpubr)\nlibrary(patchwork)\nlibrary(ggpattern)\nlibrary(data.table)\nlibrary(sf)\n\noptions(scipen=999)"
  },
  {
    "objectID": "fmm_validation.html#setup",
    "href": "fmm_validation.html#setup",
    "title": "Proportional Groundfish Redistribution",
    "section": "",
    "text": "This analysis predicts the spatial movement and net PSC change for displaced vessels under hypothetical closures of the red king crab sub savings area (RKCSSA) and validates predictions against closures in 2022 and 2023.\n\n\nCode\n#| label: setup\n#| include: false\n\n# Load packages\nlibrary(dplyr)\nlibrary(terra)\nlibrary(ggplot2)\nlibrary(lubridate)\nlibrary(tidyr)\nlibrary(units)\nlibrary(gridExtra)\nlibrary(tidyterra)\nlibrary(ggpubr)\nlibrary(patchwork)\nlibrary(ggpattern)\nlibrary(data.table)\nlibrary(sf)\n\noptions(scipen=999)"
  },
  {
    "objectID": "fmm_validation.html#set-titles",
    "href": "fmm_validation.html#set-titles",
    "title": "Proportional Groundfish Redistribution",
    "section": "2. Set Titles",
    "text": "2. Set Titles\nAssign titles, timeframe, and closures\n\n\nCode\nspp_title = case_when(\n      params$psc_spp == \"OTCR\"  ~ \"Snow Crab\",\n      params$psc_spp == \"BTCR\"  ~ \"Tanner Crab\",\n      params$psc_spp == \"RKCR\"  ~ \"Red King Crab\",\n      params$psc_spp == \"HERR\"  ~ \"Herring\",\n      TRUE                          ~ NA # Default: NA string\n    )\n\n\ntitle_name = case_when(\n      params$closure == \"smihca\"  ~ \"Saint Matthews HCA\",\n      params$closure == \"nunivak\"  ~ \"Nunivak HCA\",\n      params$closure == \"rkcssa\"  ~ \"Red King Crab Sub Savings Area\",\n      TRUE                          ~ NA # Default: NA string\n    )"
  },
  {
    "objectID": "fmm_validation.html#spatial-data-and-grid-setup",
    "href": "fmm_validation.html#spatial-data-and-grid-setup",
    "title": "Proportional Groundfish Redistribution",
    "section": "3. Spatial Data and Grid Setup",
    "text": "3. Spatial Data and Grid Setup\nLoading the Alaska background and statistical area shapefiles to create the time-series grid.\n\n\nCode\n#| label: load-spatial\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\n\n# Ensure it stays a character string and strip any accidental whitespace\nstat_areas$STAT_AREA &lt;- trimws(as.character(stat_areas$STAT_AREA))\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in params$timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "fmm_validation.html#closure-assignment",
    "href": "fmm_validation.html#closure-assignment",
    "title": "Proportional Groundfish Redistribution",
    "section": "4. Closure Assignment",
    "text": "4. Closure Assignment\nDefine the closure area based on a polygon. This step uses an overlay threshold of 10% to determine if a closure includes a stat_area, rather than just touching it. This step also establishes the surrounding stat_areas to use for the analysis, based on a buffer parameter chosen (here, 100 km).\n\n\nCode\n#| label: closure-setup\n\n# 1. Load the polygons using terra\nclosure_polygon       &lt;- terra::vect(paste0(\"shapefiles/\", params$closure, \".shp\"))\nstat_areas_with_area  &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\") \n\n# Ensure character type right away and calculate individual cell base areas\nstat_areas_with_area$STAT_AREA &lt;- trimws(as.character(stat_areas_with_area$STAT_AREA))\nstat_areas_with_area$cell_base_area &lt;- terra::expanse(stat_areas_with_area)\n\n# 2. Project stat areas if they don't match the closure polygon CRS\nif (crs(stat_areas_with_area) != crs(closure_polygon)) {\n  stat_areas_with_area &lt;- terra::project(stat_areas_with_area, crs(closure_polygon))\n}\n\n# 3. Build a geometric area overlap intersection\nintersected_geometry &lt;- terra::intersect(stat_areas_with_area, closure_polygon)\n\n# 4. FIXED: Calculate what % of the LOCAL STAT AREA CELL is covered by the closure\nintersected_geometry$overlap_area &lt;- terra::expanse(intersected_geometry)\nintersected_geometry$pct_of_cell_covered &lt;- (intersected_geometry$overlap_area / intersected_geometry$cell_base_area) * 100\n\n# 5. Extract valid stat areas based on local coverage \n# (Using 1.0% here catches any cell where the closure cuts through a meaningful chunk)\nthreshold_pct       &lt;- 10.0\nvalid_intersections &lt;- as.data.frame(intersected_geometry) %&gt;% \n  filter(pct_of_cell_covered &gt; threshold_pct)\n\nvalid_stat_areas   &lt;- unique(as.character(valid_intersections$STAT_AREA))\nclosure.stat.areas &lt;- trimws(valid_stat_areas)\n\n# 6. Separate the dataset into closure and non-closure polygons\nis_overlapping &lt;- stat_areas_with_area$STAT_AREA %in% valid_stat_areas\n\n# Isolate statistical areas meeting the threshold requirements\nclosure        &lt;- stat_areas_with_area[is_overlapping, ]\nclosure$closed &lt;- \"yes\"\nclosure2       &lt;- terra::aggregate(closure)\n\n# Isolate statistical areas that fell below the threshold requirements\nnon_closure        &lt;- stat_areas_with_area[!is_overlapping, ]\nnon_closure$closed &lt;- \"no\"\n\n# ==============================================================================\n# Generate the 100 km spatial buffer around the true aggregated closure\n# ==============================================================================\nclosure_buffer &lt;- terra::buffer(closure2, width = params$buffer) \n\n# Identify which stat areas fall within this 100 km window\nlocal_stat_matches &lt;- terra::relate(stat_areas_with_area, closure_buffer, \"intersects\")\nlocal_stat_areas   &lt;- stat_areas_with_area$STAT_AREA[as.vector(local_stat_matches)]\n\n# 7. Quick visual validation check\nplot(stat_areas_with_area[stat_areas_with_area$STAT_AREA %in% local_stat_areas, ], border=\"gray80\")\nplot(closure, border = \"blue\", lwd = 2, add = TRUE)\nplot(closure_polygon, border = \"red\", lwd = 1.5, add = TRUE)\nplot(closure_buffer, border = \"purple\", lty = 2, lwd = 1.5, add = TRUE)"
  },
  {
    "objectID": "fmm_validation.html#catch-data",
    "href": "fmm_validation.html#catch-data",
    "title": "Proportional Groundfish Redistribution",
    "section": "5. Catch Data",
    "text": "5. Catch Data\nLoad catch data from the catch accounting system (CAS) and assign to stat_areas.\n\n\nCode\n#| label: catch-data\n\n# 1. Read your pre-packaged standalone species file \ncatch_data_raw &lt;- read.csv(paste0(\"data/catch_data_\", params$psc_spp, \".csv\"), header = TRUE)\ncolnames(catch_data_raw) &lt;- toupper(colnames(catch_data_raw))\n\n# 2. Slice out data matching parameters and dynamically bin dates based on YAML parameter\ncatch_data &lt;- catch_data_raw %&gt;%\n  dplyr::filter(\n    YEAR %in% params$timeframe,\n    SPECIES_GROUP_CODE == params$psc_spp,\n    PROCESSING_SECTOR == \"CP\"\n  ) %&gt;%\n  mutate(\n    STAT_AREA = stringr::str_pad(trimws(as.character(STAT_AREA)), width = 6, side = \"left\", pad = \"0\"),\n    \n    # Force clean date classes\n    CATCH_ACTIVITY_DATE = as.Date(CATCH_ACTIVITY_DATE),\n    WEEK_END_DATE       = as.Date(WEEK_END_DATE),\n    \n    # === THE DYNAMIC TIME-STEP SWITCH ===\n    # This assigns a universal grouping key based on what you chose at the top of the file\n    TIME_STEP_KEY = case_when(\n      params$time_step == \"annual\"  ~ as.character(YEAR),\n      params$time_step == \"monthly\" ~ format(CATCH_ACTIVITY_DATE, \"%Y-%m\"),\n      TRUE                          ~ as.character(WEEK_END_DATE) # Default: weekly string\n    )\n  )\n\n# 3. Collapse multiple daily entries into the chosen time step *before* doing spatial math\n# This prevents inflating weights when changing time horizons\ncatch_data_binned &lt;- catch_data %&gt;%\n  group_by(YEAR, TIME_STEP_KEY, STAT_AREA, PROCESSING_SECTOR, SPECIES_GROUP_CODE) %&gt;%\n  summarise(\n    # Aggregate to prevent duplicating values inside identical spatial blocks\n    GF_TOTAL_CATCH_MT = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    PSC_VALUE         = sum(PSC_VALUE, na.rm = TRUE),\n    \n    # Pass a single baseline date along for Section 7's closure timeline comparison\n    CATCH_ACTIVITY_DATE = min(CATCH_ACTIVITY_DATE),\n    .groups = \"drop\"\n  )\n\n# 4. Align the master spatial object and merge\nstat_areas_with_area$STAT_AREA &lt;- stringr::str_pad(trimws(as.character(stat_areas_with_area$STAT_AREA)), width = 6, side = \"left\", pad = \"0\")\n\n# Merge catch data with spatial dataframe\ncatch_data_full &lt;- merge(stat_areas_with_area, catch_data_binned, by = \"STAT_AREA\", all.x = FALSE)\n\n# Subset master catch data strictly to your 100km buffer zone\ncatch_data2 &lt;- subset(catch_data_full, catch_data_full$STAT_AREA %in% local_stat_areas)"
  },
  {
    "objectID": "fmm_validation.html#visualizations",
    "href": "fmm_validation.html#visualizations",
    "title": "Proportional Groundfish Redistribution",
    "section": "6. Visualizations",
    "text": "6. Visualizations\nExample of weekly groundfish catch.\n\n\nCode\n#| label: fig-gf-catch\n#| fig-cap: \"Simulated weekly groundfish catch distributions.\"\n#| fig-width: 12\n#| fig-height: 8\nggplot() +\n  tidyterra::geom_spatvector(data = subset(catch_data2, catch_data2$YEAR==2018), aes(fill = GF_TOTAL_CATCH_MT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Catch (MT)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~TIME_STEP_KEY, ncol=10) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\n\nExample of weekly PSC catch.\n\n\nCode\n#| label: fig-psc-catch\n#| fig-cap: \"Simulated weekly PSC distributions.\"\n#| fig-width: 12\n#| fig-height: 8\n\nggplot() +\n  tidyterra::geom_spatvector(data = subset(catch_data2, catch_data2$YEAR==2018), aes(fill = PSC_VALUE), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"PSC Catch (Number)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~TIME_STEP_KEY, ncol=10) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())"
  },
  {
    "objectID": "fmm_validation.html#assign-closure-dates-isolate-impacted-hauls",
    "href": "fmm_validation.html#assign-closure-dates-isolate-impacted-hauls",
    "title": "Proportional Groundfish Redistribution",
    "section": "7. Assign Closure Dates & Isolate Impacted Hauls",
    "text": "7. Assign Closure Dates & Isolate Impacted Hauls\nFor full-year closures, January 1 was chosen.\n\n\nCode\n#| label: impacted-hauls\n#| echo: false\n\n#sum status quo PSC by year for later joins\nall_psc_status_quo_annual &lt;- as.data.frame(catch_data2) %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    sum_psc_status_quo = sum(PSC_VALUE)\n  )\n\n#assign dates to data ---\n# Automatically generate January 1st for every year in your global parameter timeline\nclosure_dates &lt;- data.frame(\n  YEAR = params$timeframe,\n  closure_date = paste0(params$timeframe, \"-01-01\")\n)\n\n# Ensure the new column is explicitly treated as a clean Date class\nclosure_dates$closure_date &lt;- as.Date(closure_dates$closure_date)\n\n#assign dates (Joined strictly by YEAR so it applies to all processing sectors)\nall_psc_closures_dates &lt;- as.data.frame(dplyr::left_join(catch_data2, closure_dates, by=c(\"YEAR\")))\n\n\n#assign groundfish and psc based on date ---\nall_psc_closures_dates$closure &lt;- ifelse(!is.na(all_psc_closures_dates$closure_date), \"yes\", \"no\")\nall_psc_closures_dates$impacted &lt;- ifelse(all_psc_closures_dates$CATCH_ACTIVITY_DATE &gt;= all_psc_closures_dates$closure_date, \"yes\", \"no\")\nall_psc_closures_dates$tons_gf_after_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"yes\", all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\nall_psc_closures_dates$tons_gf_no_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"no\" | is.na(all_psc_closures_dates$impacted), all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\n\n\n#isolate impacted hauls\nall_psc_impacted &lt;- subset(all_psc_closures_dates, impacted==\"yes\")\n\n# ==============================================================================\n# Final Ledger Prep: Create ledger_df_clean for validation blocks\n# ==============================================================================\n\nledger_df_clean &lt;- as.data.frame(all_psc_closures_dates) %&gt;%\n  dplyr::mutate(\n    # Ensure spatial codes are clean 6-character strings\n    STAT_AREA = stringr::str_pad(trimws(as.character(STAT_AREA)), width = 6, side = \"left\", pad = \"0\"),\n    # Identify if a row belongs inside the active regulatory box\n    inside    = ifelse(STAT_AREA %in% closure.stat.areas, \"yes\", \"no\")\n  ) %&gt;%\n  # Restrict evaluation strictly to your 100km buffer footprint outside the closure\n  dplyr::filter(STAT_AREA %in% local_stat_areas, inside == \"no\")"
  },
  {
    "objectID": "fmm_validation.html#calculate-catch-and-proportions-insideoutside-closure",
    "href": "fmm_validation.html#calculate-catch-and-proportions-insideoutside-closure",
    "title": "Proportional Groundfish Redistribution",
    "section": "8. Calculate Catch and Proportions Inside/Outside Closure",
    "text": "8. Calculate Catch and Proportions Inside/Outside Closure\nThis step summarizes the data used in the main calculations - the proportions of outside groundish catch where displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch, the total catch inside the closure to be applied to the proportions, the new PSC as a result of the new GF catch applied to local PSC rates, and the PSC caught inside the closure that will be subtracted from the net total.\n\n\nCode\n#| label: rates-props\n#| echo: false\n\n# Identify Inside/Outside after a closure would have occurred  ---\n\n# Ensure your closed area matching vector is treated as clean characters\nstat_areas_closed &lt;- as.character(closure.stat.areas) \n\n# Identify which hauls occurred inside/outside the closed area\nall_psc_impacted_cluster &lt;- all_psc_impacted %&gt;%\n  # Safe character-to-character array matching\n  mutate(inside = case_when(trimws(as.character(STAT_AREA)) %in% stat_areas_closed ~ \"yes\", TRUE ~ NA_character_))\n\nall_psc_impacted_cluster$inside[is.na(all_psc_impacted_cluster$inside)] &lt;- \"no\" \n\n\n# -- inside catch -- #\ngf_inside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"yes\")\n\ngf_inside_cluster2 &lt;- gf_inside_cluster %&gt;%\n  group_by(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_inside_wk = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    psc_measure_inside_wk = sum(PSC_VALUE, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\n# -- outside catch -- #\ngf_outside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"no\")\n\ngf_outside_cluster2 &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_outside_wk = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    psc_measure_outside_wk = sum(PSC_VALUE, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\n\n# summarize mean psc rates and calculate ratio of means  ---\nsum.stat.timeseries.gf_all &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, TIME_STEP_KEY, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_wk = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    mean_psc_wk = mean(PSC_VALUE, na.rm = TRUE),\n    mean_gf_tons_wk = mean(GF_TOTAL_CATCH_MT, na.rm = TRUE)) \n\nsum.stat.timeseries.gf_all$mean_psc_rate_wk &lt;- ifelse(\n  sum.stat.timeseries.gf_all$mean_gf_tons_wk &gt; 0,\n  sum.stat.timeseries.gf_all$mean_psc_wk / sum.stat.timeseries.gf_all$mean_gf_tons_wk,\n  0\n)\n\n\n# Proportions by YEAR and STAT_AREA  ---\ngf.outside.stat &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, TIME_STEP_KEY, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(tons_gf_annual_stat = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE))\n\ngf.outside.annually &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR) %&gt;%\n  reframe(tons_gf_annual = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE))\n\nsum.stat.timeseries.gf_all2 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all, gf.outside.annually, by = c(\"YEAR\", \"TIME_STEP_KEY\", \"PROCESSING_SECTOR\"))\nsum.stat.timeseries.gf_all3 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all2, gf.outside.stat, by = c(\"YEAR\",\"TIME_STEP_KEY\", \"STAT_AREA\", \"PROCESSING_SECTOR\"))\n\nsum.stat.timeseries.gf_all3$proportion_outside_wk &lt;- ifelse(\n  sum.stat.timeseries.gf_all3$tons_gf_annual &gt; 0,\n  sum.stat.timeseries.gf_all3$tons_gf_annual_stat / sum.stat.timeseries.gf_all3$tons_gf_annual,\n  0\n)\n\nsum.stat.timeseries.gf_all3 &lt;- as.data.frame(sum.stat.timeseries.gf_all3)"
  },
  {
    "objectID": "fmm_validation.html#plot-weekly-proportions",
    "href": "fmm_validation.html#plot-weekly-proportions",
    "title": "Proportional Groundfish Redistribution",
    "section": "9. Plot Weekly Proportions",
    "text": "9. Plot Weekly Proportions\nThis plot shows the proportions of groundfish catch in the stat areas outside of the closure within the buffer. These proportions will be what displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch.\n\n\nCode\n#| label: fig-props\n#| fig-cap: \"Observed groundfish redistribution allocation proportions.\"\n#| fig-width: 12\n#| fig-height: 8\n\n# Proportion join\nsum.stat.timeseries.gf_all_shp &lt;- dplyr::left_join(stat.areas.timeseries, sum.stat.timeseries.gf_all3, by=c(\"YEAR\", \"STAT_AREA\"))\nsum.stat.timeseries.gf_all_shp &lt;- terra::na.omit(sum.stat.timeseries.gf_all_shp, \"PROCESSING_SECTOR\")\n\npropdat &lt;- sum.stat.timeseries.gf_all_shp\npropdat$label &lt;- round(propdat$proportion_outside_wk, 2)\n\nggplot() +\n  tidyterra::geom_spatvector(data=subset(propdat, propdat$YEAR==2015, propdat$proportion_outside_wk&gt;0), aes(fill=proportion_outside_wk), alpha = 0.8, col=NA, na.rm=TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Proportion of GF Catch\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.6) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~TIME_STEP_KEY, ncol=10) +\n  theme_bw() + \n  theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())"
  },
  {
    "objectID": "fmm_validation.html#calculate-weekly-and-annual-summary-statistics",
    "href": "fmm_validation.html#calculate-weekly-and-annual-summary-statistics",
    "title": "Proportional Groundfish Redistribution",
    "section": "10. Calculate Weekly and Annual Summary Statistics",
    "text": "10. Calculate Weekly and Annual Summary Statistics\nThis step applies the displacement and new PSC calculations.\n\n\nCode\n#| label: summary-stats\n#| echo: false\n\n# Join catch from inside area\ntotal.join &lt;- dplyr::left_join(sum.stat.timeseries.gf_all3,\n                               gf_inside_cluster2 %&gt;% dplyr::select(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR, tons_gf_inside_wk),\n                               by=c(\"YEAR\", \"TIME_STEP_KEY\",  \"PROCESSING_SECTOR\"))\n\n# NAs to 0s to remove weeks where no catch happened inside the closed area\ntotal.join$tons_gf_inside_wk[is.na(total.join$tons_gf_inside_wk)] &lt;- 0\n\n# Multiply weekly displaced catch by the outside proportions\ntotal.join$displaced_gf_catch_wk &lt;- total.join$tons_gf_inside_wk * total.join$proportion_outside_wk\n\n# Multiply PSC rate by new gf catch\ntotal.join$new_PSC_wk &lt;- total.join$displaced_gf_catch_wk * total.join$mean_psc_rate_wk\n\n\n# Summarize by week and sector\n# Note: sum(unique(...)) prevents multi-sector records from over-inflating the baseline displacement total\ntotal.join2 &lt;- total.join %&gt;% \n  group_by(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_inside_wk = sum(unique(tons_gf_inside_wk)), \n    displaced_gf_catch_wk = sum(displaced_gf_catch_wk),\n    new_PSC_wk = sum(new_PSC_wk),\n    mean_psc_rate_wk = mean(mean_psc_rate_wk))\n\n# Join the status quo inside psc numbers\ntotal.join3 &lt;- dplyr::left_join(total.join2,\n                                gf_inside_cluster2 %&gt;% dplyr::select(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR, psc_measure_inside_wk),\n                                by=c(\"YEAR\", \"TIME_STEP_KEY\", \"PROCESSING_SECTOR\"))\n# NAs to 0s\ntotal.join3$psc_measure_inside_wk[is.na(total.join3$psc_measure_inside_wk)] &lt;- 0\n\n# Subtract the PSC caught inside the area\ntotal.join3$net_change_PSC_wk &lt;- total.join3$new_PSC_wk - total.join3$psc_measure_inside_wk\n\ntotal.join.plot &lt;- total.join3\n\n\n# Sum est changes for annual numbers\ntotal.join.plot.annual &lt;- total.join.plot %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    est_change_fleet = sum(net_change_PSC_wk)\n  )"
  },
  {
    "objectID": "fmm_validation.html#bootstrap",
    "href": "fmm_validation.html#bootstrap",
    "title": "Proportional Groundfish Redistribution",
    "section": "11. Bootstrap",
    "text": "11. Bootstrap\nTo provide a measure of uncertainty, we are bootstrapping 1,000 alternative scenarios by randomly shuffling the PSC rates where fishing is redistributed. We calculated a 95% Confidence Interval using the middle 95% of all simulated outcomes (between 2.5th and 97.5th percentiles).\n\n\nCode\n# ==============================================================================\n# SPATIAL BYCATCH RATE BOOTSTRAP \n# ==============================================================================\n#| label: spatial-rate-bootstrap\n\nset.seed(345)\nn_iterations &lt;- 1000\ntime_steps   &lt;- unique(total.join$TIME_STEP_KEY)\n\n# Matrix to store results (Rows = Iterations, Cols = Time Steps)\nboot_matrix &lt;- matrix(NA, nrow = n_iterations, ncol = length(time_steps))\ncolnames(boot_matrix) &lt;- time_steps\n\n# FIX: Explicitly pull psc_measure_inside_wk from gf_inside_cluster2 during setup\ndf_bs_base &lt;- total.join %&gt;%\n  dplyr::left_join(\n    gf_inside_cluster2 %&gt;% dplyr::select(YEAR, TIME_STEP_KEY, PROCESSING_SECTOR, psc_measure_inside_wk),\n    by = c(\"YEAR\", \"TIME_STEP_KEY\", \"PROCESSING_SECTOR\")\n  ) %&gt;%\n  dplyr::mutate(\n    psc_measure_inside_wk = tidyr::replace_na(psc_measure_inside_wk, 0),\n    displaced_gf_catch_wk = tons_gf_inside_wk * proportion_outside_wk\n  )\n\nfor(i in 1:n_iterations) {\n  \n  # Vector to temporarily store the randomized PSC calculations for this iteration\n  new_psc_iteration &lt;- numeric(nrow(df_bs_base))\n  \n  for(ts in time_steps) {\n    # Isolate the row indices belonging to this specific week/month step\n    ts_indices &lt;- which(df_bs_base$TIME_STEP_KEY == ts)\n    \n    # Extract the pool of observed rates for this specific time frame\n    psc_pool &lt;- df_bs_base$mean_psc_rate_wk[ts_indices]\n    \n    # If the pool is valid, randomly shuffle the rates across the active areas\n    if(length(psc_pool) &gt; 0) {\n      sampled_rates &lt;- sample(psc_pool, size = length(ts_indices), replace = TRUE)\n      new_psc_iteration[ts_indices] &lt;- df_bs_base$displaced_gf_catch_wk[ts_indices] * sampled_rates\n    }\n  }\n  \n  # Summarize the net impact for this specific shuffle loop\n  iter_summary &lt;- df_bs_base %&gt;%\n    dplyr::mutate(new_psc_boot = new_psc_iteration) %&gt;%\n    dplyr::group_by(TIME_STEP_KEY) %&gt;%\n    dplyr::summarise(\n      net_change = sum(new_psc_boot, na.rm = TRUE) - max(psc_measure_inside_wk, na.rm = TRUE),\n      .groups = \"drop\"\n    )\n  \n  # Map iteration calculations into our tracking matrix\n  for(j in seq_along(time_steps)) {\n    boot_matrix[i, j] &lt;- iter_summary$net_change[iter_summary$TIME_STEP_KEY == time_steps[j]]\n  }\n}\n\n# Transform the operational matrix into a long format data frame\nboot_long &lt;- as.data.frame(boot_matrix) %&gt;%\n  dplyr::mutate(iteration = 1:n()) %&gt;%\n  tidyr::pivot_longer(-iteration, names_to = \"TIME_STEP_KEY\", values_to = \"net_change\")\n\n# Link calendar YEARS back to our time-step keys for clear annual grouping\nyear_mapping &lt;- total.join %&gt;% \n  dplyr::select(TIME_STEP_KEY, YEAR) %&gt;% \n  dplyr::distinct()\n\nboot_annual_dist &lt;- boot_long %&gt;%\n  dplyr::left_join(year_mapping, by = \"TIME_STEP_KEY\") %&gt;%\n  dplyr::group_by(iteration, YEAR) %&gt;%\n  dplyr::summarise(annual_psc_change = sum(net_change, na.rm = TRUE), .groups = \"drop\")\n\n# Calculate the final formal 95% Confidence Bounds (2.5th and 97.5th percentiles)\nboot_annual_ci &lt;- boot_annual_dist %&gt;%\n  dplyr::group_by(YEAR) %&gt;%\n  dplyr::summarise(\n    lower_change = quantile(annual_psc_change, 0.025, na.rm = TRUE),\n    upper_change = quantile(annual_psc_change, 0.975, na.rm = TRUE),\n    .groups = \"drop\"\n  )"
  },
  {
    "objectID": "fmm_validation.html#evaluate-impacts",
    "href": "fmm_validation.html#evaluate-impacts",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Evaluate Impacts",
    "text": "12. Evaluate Impacts\nCompares the estimated PSC change to the observed PSC (status quo) each year. For this example, summarized by year.\n\n\nCode\n#| label: fig-status-quo\n#| fig-width: 12\n#| fig-height: 5\n\n# --- total by year (only estimated change) with status quo numbers --- #\ntotal.join.plot.annual2 &lt;- dplyr::left_join(all_psc_status_quo_annual, total.join.plot.annual, by = c(\"YEAR\"))\ntotal.join.plot.annual2$est_change_fleet[is.na(total.join.plot.annual2$est_change_fleet)] &lt;- 0\ntotal.join.plot.annual2$net_new_PSC &lt;- total.join.plot.annual2$sum_psc_status_quo + total.join.plot.annual2$est_change_fleet\ntotal.join.plot.annual2$LABEL &lt;- paste0(\"SPECIES\")\ncolnames(total.join.plot.annual2) &lt;- c(\"YEAR\", \"psc_statusquo\", \"psc_change\", \"net_new_PSC\", \"LABEL\")\n\n# STEP 1: Join and calculate the upper/lower bounds BEFORE pivoting the data\ntotal.join.plot.annual2 &lt;- total.join.plot.annual2 %&gt;%\n  dplyr::left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  dplyr::mutate(\n    boot_ymin = psc_statusquo + lower_change,\n    boot_ymax = psc_statusquo + upper_change\n  )\n\n# STEP 2: Pivot into long format (keeping our new boot columns intact)\ntotal.join.plot.annual3 &lt;- total.join.plot.annual2 %&gt;% pivot_longer(\n  cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n  names_to = \"psc_type\",\n  names_prefix = \"psc_\",\n  values_to = \"psc_amount\",\n  values_drop_na = TRUE\n)\n\n# STEP 3: Assign the error bar values exclusively to the net_new_PSC bar rows\ntotal.join.plot.annual3 &lt;- total.join.plot.annual3 %&gt;%\n  dplyr::mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", boot_ymin, NA_real_),\n    ymax = ifelse(psc_type == \"net_new_PSC\", boot_ymax, NA_real_)\n  )\n\n# #plot (Updated x to factor(YEAR) to align real historical timelines cleanly)\nggplot(total.join.plot.annual3, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) +\n  ggtitle(paste0(spp_title, \": \", title_name)) +\n  geom_bar(stat = \"identity\", position = position_dodge(width = 0.7), width = 0.7) +\n  geom_errorbar(\n    aes(ymin = ymin, ymax = ymax, group = psc_type),\n    position = position_dodge(width = 0.7),\n    width = 0.2,\n    color = \"black\",\n    linewidth = 0.7,\n    na.rm = TRUE\n  ) +\n  theme_bw() +\n  labs(x = \"Year\", y = \"Estimated Annual PSC (# Fish / Wt)\") +\n  scale_fill_brewer(palette = \"Paired\", name = \"Closure Scenario\", labels = c(\"RKCSSA\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5))"
  },
  {
    "objectID": "fmm_validation.html#summary-statistics",
    "href": "fmm_validation.html#summary-statistics",
    "title": "Proportional Groundfish Redistribution",
    "section": "13. Summary Statistics",
    "text": "13. Summary Statistics\n\n\nCode\n#| label: final-table\n#| fig-width: 8\n#| fig-height: 5\n\n# Final summary table\nfinal.summary.table &lt;- total.join.plot.annual2 %&gt;% dplyr::select(YEAR, psc_statusquo, net_new_PSC, psc_change)\nfinal.summary.table &lt;- as.data.frame(final.summary.table)\ncolnames(final.summary.table) &lt;- c(\"year\", \"PSC_status_quo\", \"PSC_closure\", \"Net_Change\")\n\n# Safe percentage calculation handling potential zero-activity years\nfinal.summary.table$percent_change &lt;- ifelse(\n  final.summary.table$PSC_status_quo &gt; 0,\n  (final.summary.table$Net_Change / final.summary.table$PSC_status_quo) * 100,\n  0\n)\n\nprint(final.summary.table)\n\n\n   year PSC_status_quo PSC_closure Net_Change percent_change\n1  2009      11816.850   17038.413  5221.5630     44.1874357\n2  2010      52715.714   48096.934 -4618.7799     -8.7616758\n3  2011      33169.110   29788.854 -3380.2561    -10.1909761\n4  2012      26056.641   23344.528 -2712.1128    -10.4085284\n5  2013      24340.240   25219.700   879.4596      3.6131918\n6  2014      27105.186   21272.456 -5832.7301    -21.5188709\n7  2015      12171.259   10417.247 -1754.0117    -14.4110949\n8  2016      24752.354   21684.126 -3068.2284    -12.3957034\n9  2017      32504.310   32888.109   383.7994      1.1807647\n10 2018      13026.541   12752.597  -273.9441     -2.1029685\n11 2019      25578.564   24831.057  -747.5070     -2.9223962\n12 2020      42608.105   42450.730  -157.3749     -0.3693543\n13 2021      20019.654   19914.255  -105.3990     -0.5264775\n14 2022       2646.726    2646.726     0.0000      0.0000000\n15 2023       2768.308    2768.308     0.0000      0.0000000\n16 2024      10696.771   10532.235  -164.5358     -1.5381823\n17 2025      10146.868    9835.248  -311.6198     -3.0710935"
  },
  {
    "objectID": "fmm_validation.html#validate-movement---1-year-lag",
    "href": "fmm_validation.html#validate-movement---1-year-lag",
    "title": "Proportional Groundfish Redistribution",
    "section": "14. Validate Movement - 1-Year Lag",
    "text": "14. Validate Movement - 1-Year Lag\nTo validate the accuracy of the model’s displacement predictions, we use a 1-year time lag to compare the proportion of groundfish catch in the outside stat_areas after a closure to the actual observations the following year under actual closures in 2022 and 2023. For this example, we show a correlation of 0.66 in the first year of the closure, and 0.89 in the second. This supports the use of historical catch proportions as a proxy for predicting where vessels with go when faced with a closure.\n\n\nCode\n# ==============================================================================\n# LAGGED PREDICTOR BASELINE (Prior Years: 2021 & 2022)\n# ==============================================================================\n#| label: predictive-baseline-lagged\n\nprop_df_clean &lt;- as.data.frame(propdat)\n\n# Extract the prior years' distributions using your exact structural layout\nannual_spatial_distribution_lagged &lt;- prop_df_clean %&gt;%\n  dplyr::filter(YEAR %in% c(2021, 2022)) %&gt;% \n  dplyr::group_by(YEAR, STAT_AREA, PROCESSING_SECTOR) %&gt;% \n  dplyr::summarise(\n    annual_mean_stat_prop_prior = mean(proportion_outside_wk, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  # BRIDGE STEP: Advance the year value by +1 so it aligns with the target validation year\n  dplyr::mutate(\n    VALIDATION_YEAR_MATCH = YEAR + 1\n  )\n\n# ==============================================================================\n# ACTUAL OBSERVED (Validation Years: 2022 & 2023)\n# ==============================================================================\n#| label: validation-footprint\n\n# Isolate your validation years using the exact same structure\nannual_spatial_distribution_validation &lt;- prop_df_clean %&gt;%\n  dplyr::filter(YEAR %in% c(2022, 2023)) %&gt;% \n  dplyr::group_by(YEAR, STAT_AREA, PROCESSING_SECTOR) %&gt;%  \n  dplyr::summarise(\n    annual_mean_stat_prop_actual = mean(proportion_outside_wk, na.rm = TRUE),\n    .groups = \"drop\"\n  )\n\n# ==============================================================================\n# DIRECT VALIDATION JOIN & PLOT (Prior-Year Lagged Evaluation)\n# ==============================================================================\n#| label: validation-diagnostics\n\n# Join them by aligning the actual validation year with the lagged predictor's match key\nvalidation_master_table &lt;- annual_spatial_distribution_validation %&gt;%\n  dplyr::inner_join(\n    annual_spatial_distribution_lagged, \n    by = c(\"YEAR\" = \"VALIDATION_YEAR_MATCH\", \"STAT_AREA\", \"PROCESSING_SECTOR\")\n  )\n\n# Calculate simple accuracy metrics comparing actual distributions to the prior year's footprint\nvalidation_metrics &lt;- validation_master_table %&gt;%\n  dplyr::group_by(YEAR, PROCESSING_SECTOR) %&gt;%\n  dplyr::summarise(\n    RMSE = sqrt(mean((annual_mean_stat_prop_actual - annual_mean_stat_prop_prior)^2, na.rm = TRUE)),\n    Correlation = cor(annual_mean_stat_prop_actual, annual_mean_stat_prop_prior, use = \"complete.obs\"),\n    .groups = \"drop\"\n  )\n\nprint(as.data.frame(validation_metrics))\n\n\n  YEAR PROCESSING_SECTOR       RMSE Correlation\n1 2022                CP 0.11549405   0.6576670\n2 2023                CP 0.08128407   0.8928934\n\n\nCode\n# Plot the short-term memory model fit\nggplot(validation_master_table, aes(x = annual_mean_stat_prop_prior, y = annual_mean_stat_prop_actual, color = factor(YEAR))) +\n  geom_point(size = 3.5, alpha = 0.8) +\n  geom_abline(intercept = 0, slope = 1, linetype = \"dashed\", color = \"gray40\") +\n  theme_bw() +\n  labs(\n    title = paste0(\"Prior-Year Lagged Validation Fit: \", title_name),\n    x = \"Prior Year Expected Proportions (2021 & 2022)\",\n    y = \"Actual Observed Proportions (2022 & 2023)\",\n    color = \"Closure Year\"\n  ) +\n  theme(plot.title = element_text(face = \"bold\", hjust = 0.5))"
  },
  {
    "objectID": "fmm_validation.html#how-did-our-psc-estimates-do",
    "href": "fmm_validation.html#how-did-our-psc-estimates-do",
    "title": "Proportional Groundfish Redistribution",
    "section": "15. How did our PSC estimates do?",
    "text": "15. How did our PSC estimates do?\nTo validate the accuracy of our PSC estimates under the closure, we used a historical baseline (2009-2021) of total annual PSC rates (Z_hist = PSC_hist / GF_hist) to compare to the predicted (Z_pred) and actual (Z_obs) PSC rates under the groundfish catch observed in each closure year (GF_obs). PSC catch = PSC Rate * GF Catch.\n\n\nCode\n#| label: global-volume-triple-check\n#| fig-width: 8\n#| fig-height: 5\n\n# 1. Calculate the historical aggregate baseline rate (2009-2021 Outside)\nhistorical_baseline_rate &lt;- ledger_df_clean %&gt;%\n  dplyr::filter(YEAR %in% c(2009:2021)) %&gt;%\n  dplyr::summarise(\n    total_hist_psc = sum(PSC_VALUE, na.rm = TRUE),\n    total_hist_gf  = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  dplyr::mutate(\n    mean_baseline_rate = ifelse(total_hist_gf &gt; 0, total_hist_psc / total_hist_gf, 0)\n  ) %&gt;%\n  dplyr::pull(mean_baseline_rate)\n\n# 2. Calculate the standard deviation of the historical annual rates (2009-2021 Outside)\nannual_rates_hist &lt;- ledger_df_clean %&gt;%\n  dplyr::filter(YEAR %in% c(2009:2021)) %&gt;%\n  dplyr::group_by(YEAR) %&gt;%\n  dplyr::summarise(\n    annual_psc = sum(PSC_VALUE, na.rm = TRUE),\n    annual_gf  = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  dplyr::mutate(\n    annual_rate = ifelse(annual_gf &gt; 0, annual_psc / annual_gf, 0)\n  )\n\nhistorical_baseline_sd &lt;- sd(annual_rates_hist$annual_rate, na.rm = TRUE)\nif (is.na(historical_baseline_sd)) historical_baseline_sd &lt;- 0\n\n# 3. Model-Implied Post-Closure Rate Baseline (2009-2021 Simulated)\nmodel_closure_rate &lt;- total.join %&gt;%\n  dplyr::filter(YEAR %in% c(2009:2021)) %&gt;%\n  dplyr::summarise(\n    total_model_psc = sum(new_PSC_wk, na.rm = TRUE),\n    total_model_gf  = sum(tons_gf_annual_stat, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  dplyr::mutate(\n    predicted_closure_rate = ifelse(total_model_gf &gt; 0, total_model_psc / total_model_gf, 0)\n  ) %&gt;%\n  dplyr::pull(predicted_closure_rate)\n\n# 4. Scale tracks against actual 2022/2023 landings\ntriple_volume_comparison &lt;- ledger_df_clean %&gt;%\n  dplyr::filter(YEAR %in% c(2022, 2023)) %&gt;%\n  dplyr::group_by(YEAR, PROCESSING_SECTOR) %&gt;%\n  dplyr::summarise(\n    actual_tonnage_landed = sum(GF_TOTAL_CATCH_MT, na.rm = TRUE),\n    Actual_Observed_PSC_T  = sum(PSC_VALUE, na.rm = TRUE),\n    .groups = \"drop\"\n  ) %&gt;%\n  dplyr::mutate(\n    Unconstrained_Historical_PSC_Z_hist    = actual_tonnage_landed * historical_baseline_rate,\n    Unconstrained_Historical_PSC_Z_hist_sd = actual_tonnage_landed * historical_baseline_sd,\n    Model_Predicted_PSC_Z_model            = actual_tonnage_landed * model_closure_rate\n  )\n\n# Pivot data into long format for clear multi-bar grouping\nplot_triple_data &lt;- triple_volume_comparison %&gt;%\n  dplyr::select(YEAR, Unconstrained_Historical_PSC_Z_hist, Model_Predicted_PSC_Z_model, Actual_Observed_PSC_T) %&gt;%\n  tidyr::pivot_longer(\n    cols = c(\"Unconstrained_Historical_PSC_Z_hist\", \"Model_Predicted_PSC_Z_model\", \"Actual_Observed_PSC_T\"),\n    names_to = \"Scenario_Type\",\n    values_to = \"PSC_Count\"\n  )\n\n# Join the error metrics back to the long plotting table\nplot_triple_data &lt;- plot_triple_data %&gt;%\n  dplyr::left_join(triple_volume_comparison %&gt;% dplyr::select(YEAR, Unconstrained_Historical_PSC_Z_hist_sd), by = \"YEAR\") %&gt;%\n  dplyr::mutate(\n    # Apply variance limits ONLY to the Historical bar, leave others as NA\n    ymin = ifelse(Scenario_Type == \"Unconstrained_Historical_PSC_Z_hist\", pmax(0, PSC_Count - Unconstrained_Historical_PSC_Z_hist_sd), NA_real_),\n    ymax = ifelse(Scenario_Type == \"Unconstrained_Historical_PSC_Z_hist\", PSC_Count + Unconstrained_Historical_PSC_Z_hist_sd, NA_real_)\n  )\n\n# Set up clean factor levels to dictate left-to-right bar ordering\nplot_triple_data$Scenario_Type &lt;- factor(\n  plot_triple_data$Scenario_Type,\n  levels = c(\"Unconstrained_Historical_PSC_Z_hist\", \"Model_Predicted_PSC_Z_model\", \"Actual_Observed_PSC_T\")\n)\n\n# Generate the Plot\nggplot(plot_triple_data, aes(x = factor(YEAR), y = PSC_Count, fill = Scenario_Type)) +\n  geom_bar(stat = \"identity\", position = position_dodge(width = 0.75), alpha = 0.9, width = 0.75) +\n  geom_errorbar(\n    aes(ymin = ymin, ymax = ymax, group = Scenario_Type), \n    position = position_dodge(width = 0.75),             \n    width = 0.25,\n    color = \"black\",\n    linewidth = 0.8,\n    na.rm = TRUE\n  ) +\n  scale_fill_manual(\n    values = c(\n      \"Unconstrained_Historical_PSC_Z_hist\" = \"#757575\", # Dark Grey\n      \"Model_Predicted_PSC_Z_model\"         = \"#e41a1c\", # Red\n      \"Actual_Observed_PSC_T\"               = \"#377eb8\"  # Blue\n    ),\n    labels = c(\n      \"Historical Baseline (Z_hist) ± SD\", \n      \"Model-Predicted Closure (Z_model)\", \n      \"Actual Observed Reality (T)\"\n    )\n  ) +\n  theme_bw() +\n  labs(\n    title = paste0(\"Closure Validation: \", title_name),\n    x = \"Emergency Closure Year\",\n    y = paste0(\"Total PSC (\", params$psc_spp, \" Count)\"),\n    fill = \"Scenario Track\"\n  ) +\n  theme(\n    plot.title = element_text(face = \"bold\", size = 13, hjust = 0.5),\n    plot.subtitle = element_text(face = \"italic\", size = 9, hjust = 0.5),\n    legend.position = \"bottom\",\n    legend.direction = \"vertical\",\n    legend.box = \"horizontal\"\n  )"
  },
  {
    "objectID": "bootstrap_speed_test.html#set-parameters",
    "href": "bootstrap_speed_test.html#set-parameters",
    "title": "Proportional Groundfish Redistribution",
    "section": "2. Set Parameters",
    "text": "2. Set Parameters\nAssign titles, timeframe, and closures\n\n\nCode\n# Parameters\nspp_title = \"Species\"\ntitle_name = \"Closure\"\ntimeframe &lt;- c(2011:2023)\nclosure.stat.areas &lt;- c(665730, 665700, 655730, 655700)"
  },
  {
    "objectID": "bootstrap_speed_test.html#spatial-data-and-grid-setup",
    "href": "bootstrap_speed_test.html#spatial-data-and-grid-setup",
    "title": "Proportional Groundfish Redistribution",
    "section": "3. Spatial Data and Grid Setup",
    "text": "3. Spatial Data and Grid Setup\nLoading the Alaska background and statistical area shapefiles to create the time-series grid.\n\n\nCode\n# Note: Ensure these paths are relative to your .qmd file location\nAK &lt;- terra::vect(\"shapefiles/mv_alaska_1mil_py.shp\")\nstat_areas &lt;- terra::vect(\"shapefiles/State_Stat_2004.shp\")\nstat_areas$STAT_AREA &lt;- as.numeric(stat_areas$STAT_AREA)\n\n# Create timeseries list\nstat.list &lt;- list()\nfor (i in timeframe){\n  stat_shape &lt;- stat_areas\n  stat_shape$YEAR &lt;- i\n  stat.list[[as.character(i)]] &lt;- stat_shape\n}\nstat.areas.timeseries &lt;- terra::vect(stat.list)"
  },
  {
    "objectID": "bootstrap_speed_test.html#closure-assignment",
    "href": "bootstrap_speed_test.html#closure-assignment",
    "title": "Proportional Groundfish Redistribution",
    "section": "4. Closure Assignment",
    "text": "4. Closure Assignment\nDefining the closure area based on specific statistical areas.\n\n\nCode\nclosure &lt;- subset(stat_areas, stat_areas$STAT_AREA %in% closure.stat.areas)\nclosure$closed &lt;- \"yes\"\nclosure2 &lt;- terra::aggregate(closure)\n\nnon_closure &lt;- stat_areas[!(stat_areas$STAT_AREA %in% closure.stat.areas), ]\nnon_closure$closed &lt;- \"no\""
  },
  {
    "objectID": "bootstrap_speed_test.html#simulation-data",
    "href": "bootstrap_speed_test.html#simulation-data",
    "title": "Proportional Groundfish Redistribution",
    "section": "5. Simulation Data",
    "text": "5. Simulation Data\nSimulating weekly catch data for 20 cells closest to the closure area. This is where you would also enter your own catch data. Here, we’re also simulating a bias of higher PSC inside the hypothetical closure.\n\n\nCode\n# --- 5. Simulation Data (Biased for high PSC inside closure) ---\n\nset.seed(123)\nnum_to_select &lt;- 20\n\n# 1. Calculate distances from non-closure to closure areas\ndistances_to_closure &lt;- terra::distance(non_closure, closure2)\nmin_distances &lt;- apply(distances_to_closure, MARGIN = 1, FUN = min)\nnon_closure$dist_to_closure &lt;- min_distances\n\n# 2. Select 20 areas outside but close to the closure\nnon_closure_ordered &lt;- non_closure[order(non_closure$dist_to_closure), ]\nsim_areas &lt;- non_closure_ordered %&gt;% slice(1:num_to_select)\n\n# 3. Merge back closure areas\nsim_areas &lt;- rbind(sim_areas, closure)\n\n# 4. Generate Weekly Data with PSC Bias\nsimulated_years &lt;- c(2021, 2022, 2023)\nsim.list &lt;- list()\n\nfor(i in simulated_years){\n  \n  # Create a sequence of 5 weeks starting in August for each year\n  week_sequence &lt;- seq.Date(from = as.Date(paste0(i, \"-08-01\")), \n                            by = \"week\", length.out = 5)\n  year_weeks_list &lt;- list()\n  \n  for(week_date in as.character(week_sequence)) {\n    \n    # Identify which rows are inside the closure for this specific loop\n    is_inside &lt;- sim_areas$STAT_AREA %in% closure.stat.areas\n    num_inside &lt;- sum(is_inside)\n    num_outside &lt;- sum(!is_inside)\n    \n    # Create the data frame\n    wk_dat &lt;- data.frame(\n      STAT_AREA = sim_areas$STAT_AREA,\n      YEAR = i,\n      WEEK_END_DATE = week_date,\n      CATCH_ACTIVITY_DATE = as.character(as.Date(week_date) - 2),\n      PROCESSING_SECTOR = \"X\"\n    )\n    \n    # Generate Groundfish catch (kept consistent across all areas)\n    wk_dat$GF_TOTAL_CATCH_MT &lt;- round(rnorm(nrow(sim_areas), mean = 50, sd = 10), 2)\n    \n    # Higher PSC mean for inside areas \n    wk_dat$PSC_TOTAL_COUNT[is_inside] &lt;- round(rnorm(num_inside, mean = 20, sd = 3), 2)\n    \n    # Lower PSC mean for outside areas\n    wk_dat$PSC_TOTAL_COUNT[!is_inside] &lt;- rnbinom(num_outside, mu = 10, size = 2)\n    \n    year_weeks_list[[week_date]] &lt;- wk_dat\n  }\n  sim.list[[as.character(i)]] &lt;- do.call(rbind, year_weeks_list)\n}\n\ncatch_data &lt;- do.call(rbind, sim.list)\n\n# 5. Merge catch data with spatvector\n# Using all.x = FALSE ensures the map only displays areas with data\ncatch_data2 &lt;- merge(stat_areas, catch_data, by = \"STAT_AREA\", all.x = FALSE)"
  },
  {
    "objectID": "bootstrap_speed_test.html#visualizations",
    "href": "bootstrap_speed_test.html#visualizations",
    "title": "Proportional Groundfish Redistribution",
    "section": "6. Visualizations",
    "text": "6. Visualizations\nWeekly groundfish catch - simulated to be randomly distributed across the study area (closure + 20 adjacent stat areas).\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = GF_TOTAL_CATCH_MT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Catch (MT)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 1: Simulated weekly groundfish catch distributions.\n\n\n\n\n\nWeekly PSC catch - simulated to have higher PSC within the hypothetical closure.\n\n\nCode\nggplot() +\n  tidyterra::geom_spatvector(data = catch_data2, aes(fill = PSC_TOTAL_COUNT), \n                             alpha = 0.8, col = NA, na.rm = TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"PSC Catch (Number)\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 2: Simulated weekly PSC distributions."
  },
  {
    "objectID": "bootstrap_speed_test.html#assign-closure-dates-isolate-impacted-hauls",
    "href": "bootstrap_speed_test.html#assign-closure-dates-isolate-impacted-hauls",
    "title": "Proportional Groundfish Redistribution",
    "section": "7. Assign Closure Dates & Isolate Impacted Hauls",
    "text": "7. Assign Closure Dates & Isolate Impacted Hauls\nFor this exercise, July 1 chosen as the closure date for each year. For year-long closures, choose January 1.\n\n\nCode\n#| label: impacted-hauls\n#| echo: false\n\n#sum status quo PSC by year for later joins\nall_psc_status_quo_annual &lt;- as.data.frame(catch_data2) %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    sum_psc_status_quo = sum(PSC_TOTAL_COUNT)\n  )\n\n#assign dates to data ---\n#set closure date\nclosure_dates &lt;- data.frame(\n  YEAR = c(2021,2022,2023),\n  PROCESSING_SECTOR = \"X\",\n  closure_date = c('2021-07-01','2022-07-01', '2023-07-01')\n)\n\n#assign dates\nall_psc_closures_dates &lt;- as.data.frame(dplyr::left_join(catch_data2, closure_dates, by=c(\"YEAR\", \"PROCESSING_SECTOR\")))\n\n\n#assign groundfish and psc based on date ---\nall_psc_closures_dates$closure &lt;- ifelse(!is.na(all_psc_closures_dates$closure_date), \"yes\", \"no\")\nall_psc_closures_dates$impacted &lt;- ifelse(all_psc_closures_dates$CATCH_ACTIVITY_DATE &gt; all_psc_closures_dates$closure_date, \"yes\", \"no\")\nall_psc_closures_dates$tons_gf_after_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"yes\", all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\nall_psc_closures_dates$tons_gf_no_closure &lt;- ifelse(all_psc_closures_dates$impacted==\"no\" | is.na(all_psc_closures_dates$impacted), all_psc_closures_dates$GF_TOTAL_CATCH_MT, 0)\n\n\n#isolate impacted hauls\nall_psc_impacted &lt;- subset(all_psc_closures_dates, impacted==\"yes\")"
  },
  {
    "objectID": "bootstrap_speed_test.html#calculate-catch-and-proportions-insideoutside-closure",
    "href": "bootstrap_speed_test.html#calculate-catch-and-proportions-insideoutside-closure",
    "title": "Proportional Groundfish Redistribution",
    "section": "8. Calculate Catch and Proportions Inside/Outside Closure",
    "text": "8. Calculate Catch and Proportions Inside/Outside Closure\nThis step summarizes the data used in the main calculations - the proportions of outside groundish catch where displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch, the total catch inside the closure to be applied to the proportions, the new PSC as a result of the new GF catch applied to local PSC rates, and the PSC caught inside the closure that will be subtracted from the net total.\n\n\nCode\n#| label: rates-props\n#| echo: false\n\n# Identify Inside/Outside after a closure would have occurred  ---\n\n#identify the area\nstat_areas_closed &lt;- closure.stat.areas \n\n#identify which hauls occurred inside/outside the closed area\nall_psc_impacted_cluster &lt;- all_psc_impacted %&gt;%\n  mutate(inside = case_when(STAT_AREA %in% c(stat_areas_closed) ~ \"yes\", TRUE ~ NA_character_))\nall_psc_impacted_cluster$inside[is.na(all_psc_impacted_cluster$inside)] &lt;- \"no\" # NA cpue to 0s\n\n\n# -- inside catch -- #\n#isolate hauls occurring inside closed area\ngf_inside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"yes\")\n\n#sum gf and psc inside closed area for later calcs\ngf_inside_cluster2 &lt;- gf_inside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_inside_wk = sum(GF_TOTAL_CATCH_MT),\n    psc_measure_inside_wk = sum(PSC_TOTAL_COUNT)\n  )\n\n# -- outside catch -- #\n#isolate the hauls that occurred outside the closed area\ngf_outside_cluster &lt;- subset(all_psc_impacted_cluster, inside == \"no\")\n\ngf_outside_cluster2 &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  summarise(\n    tons_gf_outside_wk = sum(GF_TOTAL_CATCH_MT),\n    psc_measure_outside_wk = sum(PSC_TOTAL_COUNT)\n  )\n\n\n# summarize mean psc rates and calculate ratio of means  ---\n\nsum.stat.timeseries.gf_all &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_wk = sum(GF_TOTAL_CATCH_MT),\n    mean_psc_wk = mean(PSC_TOTAL_COUNT),\n    mean_gf_tons_wk = mean(GF_TOTAL_CATCH_MT)) \n\n#ratio\nsum.stat.timeseries.gf_all$mean_psc_rate_wk &lt;- sum.stat.timeseries.gf_all$mean_psc_wk/sum.stat.timeseries.gf_all$mean_gf_tons_wk\n\n\n# Proportions by YEAR and STAT_AREA  ---\n\n#isolate gf catch by week\ngf.outside.stat &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE, STAT_AREA, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_annual_stat = sum(GF_TOTAL_CATCH_MT)\n  )\n\n# sum all catch outside\ngf.outside.annually &lt;- gf_outside_cluster %&gt;%\n  group_by(YEAR, WEEK_END_DATE,  PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_annual = sum(GF_TOTAL_CATCH_MT))\n\n#join the the sums back to main\nsum.stat.timeseries.gf_all2 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all, gf.outside.annually, by = c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\"))\nsum.stat.timeseries.gf_all3 &lt;- dplyr::left_join(sum.stat.timeseries.gf_all2, gf.outside.stat, by = c(\"YEAR\",\"WEEK_END_DATE\", \"STAT_AREA\", \"PROCESSING_SECTOR\"))\n\n# CALC PROPORTION OF OUTSIDE CATCH FOR EACH AREA\nsum.stat.timeseries.gf_all3$proportion_outside_wk &lt;- sum.stat.timeseries.gf_all3$tons_gf_annual_stat/sum.stat.timeseries.gf_all3$tons_gf_annual\nsum.stat.timeseries.gf_all3 &lt;- as.data.frame(sum.stat.timeseries.gf_all3)"
  },
  {
    "objectID": "bootstrap_speed_test.html#plot-weekly-proportions",
    "href": "bootstrap_speed_test.html#plot-weekly-proportions",
    "title": "Proportional Groundfish Redistribution",
    "section": "9. Plot Weekly Proportions",
    "text": "9. Plot Weekly Proportions\nThis plot shows the proportions of groundfish catch for the 20 adjacent stat areas outside of the closure. These proportions will be what displaced catch is applied to, under the assumption that the fishery will meet the same overall total catch.\n\n\nCode\n#proportion\nsum.stat.timeseries.gf_all_shp &lt;- dplyr::left_join(stat.areas.timeseries, sum.stat.timeseries.gf_all3, by=c(\"YEAR\", \"STAT_AREA\"))\nsum.stat.timeseries.gf_all_shp &lt;- terra::na.omit(sum.stat.timeseries.gf_all_shp, \"PROCESSING_SECTOR\")\n\npropdat &lt;- sum.stat.timeseries.gf_all_shp\npropdat$label &lt;- round(propdat$proportion_outside_wk, 2)\n\nggplot() +\n  tidyterra::geom_spatvector(data=subset(propdat, propdat$proportion_outside_wk&gt;0), aes(fill=proportion_outside_wk), alpha = 0.8, col=NA, na.rm=TRUE) +\n  scale_fill_distiller(palette = \"GnBu\", name = \"Proportion of GF Catch\") +\n  tidyterra::geom_spatvector(data = closure2, aes(color = \"Closure\"), \n                             fill = NA, alpha = 0.5, lwd = 0.5) +\n  scale_color_manual(values = c(\"Closure\" = \"red\"), name = NULL) +\n  facet_wrap(~WEEK_END_DATE, ncol=5) +\n  theme_bw() + theme(plot.title = element_text(hjust = 0.5), axis.title = element_blank())\n\n\n\n\n\n\n\n\nFigure 3: Simulated weekly GF Catch Proportions."
  },
  {
    "objectID": "bootstrap_speed_test.html#calculate-weekly-and-annual-summary-statistics",
    "href": "bootstrap_speed_test.html#calculate-weekly-and-annual-summary-statistics",
    "title": "Proportional Groundfish Redistribution",
    "section": "10. Calculate Weekly and Annual Summary Statistics",
    "text": "10. Calculate Weekly and Annual Summary Statistics\n\n\nCode\n#| label: summary-stats\n#| echo: false\n\n#join catch from inside area\ntotal.join &lt;- dplyr::left_join(sum.stat.timeseries.gf_all3,\n                               gf_inside_cluster2 %&gt;% dplyr::select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, tons_gf_inside_wk),\n                               by=c(\"YEAR\", \"WEEK_END_DATE\",  \"PROCESSING_SECTOR\"))\n\n#NAs to 0s to remove weeks where no catch happened inside the closed area\ntotal.join$tons_gf_inside_wk[is.na(total.join$tons_gf_inside_wk)] &lt;- 0\n\n#multiply weekly displaced catch by the outside proportions\ntotal.join$displaced_gf_catch_wk &lt;- total.join$tons_gf_inside_wk * total.join$proportion_outside_wk\n\n#multiply PSC rate by new gf catch\ntotal.join$new_PSC_wk &lt;- total.join$displaced_gf_catch_wk * total.join$mean_psc_rate_wk\n\n\n#summarize by week and sector\ntotal.join2 &lt;- total.join %&gt;% group_by(YEAR, WEEK_END_DATE, PROCESSING_SECTOR) %&gt;%\n  reframe(\n    tons_gf_inside_wk = max(tons_gf_inside_wk),\n    displaced_gf_catch_wk = sum(displaced_gf_catch_wk),\n    new_PSC_wk = sum(new_PSC_wk),\n    mean_psc_rate_wk = mean(mean_psc_rate_wk))\n\n#join the status quo inside psc numbers\ntotal.join3 &lt;- dplyr::left_join(total.join2,\n                                gf_inside_cluster2 %&gt;% dplyr::select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n                                by=c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\"))\n#NAs to 0s\ntotal.join3$psc_measure_inside_wk[is.na(total.join3$psc_measure_inside_wk)] &lt;- 0\n\n#subtract the PSC caught inside the area\ntotal.join3$net_change_PSC_wk &lt;- total.join3$new_PSC_wk - total.join3$psc_measure_inside_wk\n\ntotal.join.plot &lt;- total.join3\n\n\n#sum est changes for annual numbers\ntotal.join.plot.annual &lt;- total.join.plot %&gt;%\n  group_by(YEAR) %&gt;%\n  reframe(\n    est_change_fleet = sum(net_change_PSC_wk)\n  )"
  },
  {
    "objectID": "bootstrap_speed_test.html#bootstrap---r",
    "href": "bootstrap_speed_test.html#bootstrap---r",
    "title": "Proportional Groundfish Redistribution",
    "section": "11. Bootstrap - R",
    "text": "11. Bootstrap - R\nTo provide a measure of uncertainty, we are bootstrapping 1,000 alternative scenarios by randomly shuffling the PSC rates where fishing is redistributed. We calculated a 95% Confidence Interval using the middle 95% of all simulated outcomes (between 2.5th and 97.5th percentiles).\n\n\nCode\nstart_r &lt;- Sys.time()\n\n# Create a base dataframe that keeps STAT_AREA for resampling\ndf_bs_base &lt;- total.join %&gt;%\n  left_join(gf_inside_cluster2 %&gt;% \n              select(YEAR, WEEK_END_DATE, PROCESSING_SECTOR, psc_measure_inside_wk),\n            by = c(\"YEAR\", \"WEEK_END_DATE\", \"PROCESSING_SECTOR\")) %&gt;%\n  mutate(psc_measure_inside_wk = replace_na(psc_measure_inside_wk, 0),\n         displaced_gf_catch_wk = tons_gf_inside_wk * proportion_outside_wk)\n\nset.seed(345)\nn_iterations &lt;- 1000\nweeks &lt;- unique(df_bs_base$WEEK_END_DATE)\nboot_results &lt;- matrix(NA, nrow = n_iterations, ncol = length(weeks))\ncolnames(boot_results) &lt;- weeks\n\nfor(i in 1:n_iterations) {\n  \n  # Temporary storage for this iteration\n  new_psc_iteration &lt;- numeric(nrow(df_bs_base))\n  \n  for(w in weeks) {\n    wk_indices &lt;- which(df_bs_base$WEEK_END_DATE == w)\n    \n    # THE FIX: Sample from the pool of all rates available this week\n    psc_pool &lt;- df_bs_base$mean_psc_rate_wk[wk_indices]\n    \n    # Randomly assign a rate from the pool to each STAT_AREA\n    sampled_rates &lt;- sample(psc_pool, size = length(wk_indices), replace = TRUE)\n    \n    # Calculate new PSC: (Fixed Displaced Catch) * (Randomly Sampled Rate)\n    new_psc_iteration[wk_indices] &lt;- df_bs_base$displaced_gf_catch_wk[wk_indices] * sampled_rates\n  }\n  \n  # Summarize results for this iteration\n  df_iter &lt;- df_bs_base\n  df_iter$new_psc_boot &lt;- new_psc_iteration\n  \n  iter_summary &lt;- df_iter %&gt;%\n    group_by(WEEK_END_DATE) %&gt;%\n    summarise(\n      net_change = sum(new_psc_boot) - max(psc_measure_inside_wk),\n      .groups = \"drop\"\n    )\n  \n  for(j in seq_along(weeks)) {\n    boot_results[i, j] &lt;- iter_summary$net_change[iter_summary$WEEK_END_DATE == weeks[j]]\n  }\n}\n\n# 4. Calculate Confidence Intervals\nci_summary &lt;- data.frame(\n  WEEK_END_DATE = colnames(boot_results),\n  mean_net_change = colMeans(boot_results),\n  lower_ci = apply(boot_results, 2, quantile, probs = 0.025),\n  upper_ci = apply(boot_results, 2, quantile, probs = 0.975)\n)\n\n# 5. Merge with original modeled data for comparison\nfinal_comparison &lt;- left_join(ci_summary, total.join3 %&gt;% \n                                select(WEEK_END_DATE, net_change_PSC_wk), \n                              by = \"WEEK_END_DATE\")\n\n#summarize bootstrap data into annual\n# Convert matrix to long dataframe to join with Year info\nboot_long &lt;- as.data.frame(boot_results) %&gt;%\n  mutate(iteration = 1:n()) %&gt;%\n  pivot_longer(-iteration, names_to = \"WEEK_END_DATE\", values_to = \"net_change\") %&gt;%\n  mutate(WEEK_END_DATE = as.Date(WEEK_END_DATE),\n         YEAR = lubridate::year(WEEK_END_DATE))\n\n# Sum by iteration and year to get annual distribution of change\nboot_annual_dist &lt;- boot_long %&gt;%\n  group_by(iteration, YEAR) %&gt;%\n  summarise(annual_psc_change = sum(net_change), .groups = \"drop\")\n\n# Calculate CIs for the annual change\nboot_annual_ci &lt;- boot_annual_dist %&gt;%\n  group_by(YEAR) %&gt;%\n  summarise(\n    lower_change = quantile(annual_psc_change, 0.025),\n    upper_change = quantile(annual_psc_change, 0.975)\n  )\n\nend_r &lt;- Sys.time()\ntime_r &lt;- end_r - start_r\n\nprint(time_r)\n\n\nTime difference of 1.882519 secs"
  },
  {
    "objectID": "bootstrap_speed_test.html#bootstrap---rcpp",
    "href": "bootstrap_speed_test.html#bootstrap---rcpp",
    "title": "Proportional Groundfish Redistribution",
    "section": "11.1 Bootstrap - Rcpp",
    "text": "11.1 Bootstrap - Rcpp\nTesting speed of bootstrap implemented in C++ environment\n\n\nCode\nstart_cpp &lt;- Sys.time()\n\nlibrary(Rcpp)\nsourceCpp(\"bootstrap_psc.cpp\")\n\n# 1. Create an empty list to store results for each year\nannual_boot_list &lt;- list()\n\n# 2. Loop through each year and run the C++ function\nfor(yr in unique(df_bs_base$YEAR)) {\n  \n  # Isolate data for just this year\n  yr_data &lt;- df_bs_base[df_bs_base$YEAR == yr, ]\n  \n  # Run C++ for 1000 iterations for this specific year\n  yr_results &lt;- bootstrap_psc_cpp(\n    n_iterations = 1000,\n    displaced_gf = yr_data$displaced_gf_catch_wk,\n    psc_pool = yr_data$mean_psc_rate_wk,\n    psc_inside_wk = yr_data$psc_measure_inside_wk\n  )\n  \n  # Store in a data frame\n  annual_boot_list[[as.character(yr)]] &lt;- data.frame(\n    YEAR = yr,\n    annual_psc_change = yr_results\n  )\n}\n\n# 3. Combine all years back into one data frame\nboot_annual_dist_cpp &lt;- do.call(rbind, annual_boot_list)\n\n# 4. Now calculate your CIs (This will now work!)\nboot_annual_ci &lt;- boot_annual_dist_cpp %&gt;%\n  group_by(YEAR) %&gt;%\n  summarise(\n    lower_change = quantile(annual_psc_change, 0.025),\n    upper_change = quantile(annual_psc_change, 0.975),\n    .groups = \"drop\"\n  )\n\nend_cpp &lt;- Sys.time()\ntime_cpp &lt;- end_cpp - start_cpp\n\nprint(time_cpp)\n\n\nTime difference of 16.55454 secs"
  },
  {
    "objectID": "bootstrap_speed_test.html#status-quo-comparison",
    "href": "bootstrap_speed_test.html#status-quo-comparison",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Status Quo Comparison",
    "text": "12. Status Quo Comparison\n\n\nCode\n# --- 12. Final Status Quo Comparison ---\n\n# 1. Clean build of the annual comparison data\ntotal.join.plot.annual_clean &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # 2. THE VERTICAL FIX: \n  # Calculate the distance from the bootstrap result to the bootstrap mean\n  # then apply THAT distance to your actual bar height (net_new_PSC)\n  mutate(\n    # Find the 'spread' of the bootstrap distribution\n    boot_mean = (upper_change + lower_change) / 2, # Using the mid-point of the bootstrap\n    half_width_lower = boot_mean - lower_change,\n    half_width_upper = upper_change - boot_mean,\n    \n    # Apply that spread relative to your ACTUAL bar height\n    ymin_val = net_new_PSC - half_width_lower,\n    ymax_val = net_new_PSC + half_width_upper\n  ) %&gt;%\n  # 3. Pivot for ggplot\n  pivot_longer(\n    cols = c(\"net_new_PSC\", \"psc_statusquo\"),\n    names_to = \"psc_type\",\n    values_to = \"psc_amount\"\n  ) %&gt;%\n  mutate(\n    ymin = ifelse(psc_type == \"net_new_PSC\", ymin_val, NA),\n    ymax = ifelse(psc_type == \"net_new_PSC\", ymax_val, NA)\n  ) %&gt;%\n  distinct()\n\n# 4. Final Plot \nggplot(total.join.plot.annual_clean, aes(x = factor(YEAR), y = psc_amount, fill = psc_type)) + \n  geom_bar(stat = \"identity\", \n           position = position_dodge(width = 0.9)) + \n  geom_errorbar(aes(ymin = ymin, ymax = ymax), \n                position = position_dodge(width = 0.9), \n                width = 0.25, \n                na.rm = TRUE) +\n  # This ensures the bars start exactly at 0 and adds 15% space at the top for labels/CIs\n  scale_y_continuous(expand = expansion(mult = c(0, 0.15))) + \n  theme_bw() + \n  labs(title = paste0(spp_title, \": \", title_name),\n       x = \"Year\", \n       y = \"Estimated Annual PSC (# Fish)\") +\n  scale_fill_brewer(palette = \"Paired\", \n                    name = \"Scenario\", \n                    labels = c(\"Closure (with 95% CI)\", \"Status Quo\")) +\n  theme(plot.title = element_text(hjust = 0.5))\n\n\n\n\n\n\n\n\nFigure 4"
  },
  {
    "objectID": "bootstrap_speed_test.html#final-summary-statistics-table",
    "href": "bootstrap_speed_test.html#final-summary-statistics-table",
    "title": "Proportional Groundfish Redistribution",
    "section": "12. Final Summary Statistics Table",
    "text": "12. Final Summary Statistics Table\n\n\nCode\n# Re-align with the original script's naming convention\ntotal.join.plot.annual2 &lt;- all_psc_status_quo_annual %&gt;%\n  left_join(total.join.plot.annual, by = \"YEAR\") %&gt;%\n  rename(psc_statusquo = sum_psc_status_quo, \n         psc_change = est_change_fleet) %&gt;%\n  mutate(psc_change = replace_na(psc_change, 0),\n         net_new_PSC = psc_statusquo + psc_change) %&gt;%\n  left_join(boot_annual_ci, by = \"YEAR\") %&gt;%\n  # Add the CI bounds we calculated for the plot\n  mutate(\n    lower_diff = psc_change - lower_change,\n    upper_diff = upper_change - psc_change,\n    ymin_val = net_new_PSC - lower_diff,\n    ymax_val = net_new_PSC + upper_diff\n  )\n\n# Create the final summary table\nfinal.summary.table &lt;- total.join.plot.annual2 %&gt;% \n  dplyr::select(YEAR, psc_statusquo, net_new_PSC, ymin_val, ymax_val, psc_change)\n\n# Format for the report\nfinal.summary.table &lt;- as.data.frame(final.summary.table)\ncolnames(final.summary.table) &lt;- c(\"year\", \"PSC_status_quo\", \"PSC_closure\", \"lowerCI_closure\", \"upperCI_closure\", \"Net_Change\")\n\n# Calculate percent change\nfinal.summary.table$percent_change &lt;- (final.summary.table$Net_Change / final.summary.table$PSC_status_quo) * 100\n\n# Render as a professional HTML table for Quarto\nlibrary(knitr)\nkable(final.summary.table, \n      digits = 2, \n      caption = \"Annual PSC Comparison: Status Quo vs. Closure Scenarios\")\n\n\n\nAnnual PSC Comparison: Status Quo vs. Closure Scenarios\n\n\n\n\n\n\n\n\n\n\n\nyear\nPSC_status_quo\nPSC_closure\nlowerCI_closure\nupperCI_closure\nNet_Change\npercent_change\n\n\n\n\n2021\n1376.55\n1164.11\n-6544.89\n-6487.68\n-212.44\n-15.43\n\n\n2022\n1490.66\n1298.84\n-6570.55\n-6500.66\n-191.82\n-12.87\n\n\n2023\n1292.53\n1061.58\n-6630.93\n-6563.06\n-230.95\n-17.87"
  }
]